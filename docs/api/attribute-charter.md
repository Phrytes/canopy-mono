# `@onderling/attribute-charter` — API reference

> Generated by `node scripts/api-appendix.mjs` from the exported surface and source JSDoc.
> Do not edit by hand — edit the JSDoc in the source and regenerate. `npm run api-fitness` fails on drift.

Requested-attributes charter — a coarse, capped, k-anon-guarded privacy mini-spec for optional background attributes on pseudonymous feedback. The first concrete instance of the personal-properties/disclosure model. Pure functions, web ≡ mobile.

README: [`packages/attribute-charter/README.md`](../../packages/attribute-charter/README.md) · Index: [docs/api/README.md](README.md)

## `src/charter.js`

### `CHARTER_MAX_ATTRIBUTES`

**Kind:** constant · **Import:** `CHARTER_MAX_ATTRIBUTES` from `'@onderling/attribute-charter'`

Maximum number of (coarse) attributes one charter may request — keeps the joint value space small.

### `createCharter`

**Kind:** function · **Import:** `createCharter` from `'@onderling/attribute-charter'`

```js
createCharter({ projectId, version = 1, attributes } = {})
```

Build a canonical, validated charter.

**Parameters**

- `input` `{projectId: string, version?: number, attributes: Array<{key: string, purpose: string}>}`

**Returns:** `{projectId: string, version: number, attributes: Array<{key: string, purpose: string}>}`

### `charterHash`

**Kind:** function · **Import:** `charterHash` from `'@onderling/attribute-charter'`

```js
charterHash(charter)
```

Deterministic content hash of a charter (projectId + version + attributes).
A signature (project pubkey) signs THIS value at integration; contributions
carry it so the agreed charter is provable + tamper-evident.

**Parameters**

- `charter` `object` — a charter (from createCharter, or the same shape)

**Returns:** `string` — lowercase hex sha-256

### `charterKeys`

**Kind:** function · **Import:** `charterKeys` from `'@onderling/attribute-charter'`

```js
charterKeys(charter)
```

The set of attribute keys a charter requests.

## `src/deviceWarning.js`

### `disclosureWarning`

**Kind:** function · **Import:** `disclosureWarning` from `'@onderling/attribute-charter'`

```js
disclosureWarning({ enabledKeys = [], n, mode = 'normal' } = {})
```

On-device disclosure warning: warns when the participant's enabled attribute combo is likely
rare in a cohort of size `n` — i.e. ≥2 keys enabled AND their possible-combo space (product of
bucket counts) exceeds the threshold (`n` in 'normal' mode, `n × MINIMAL_FACTOR` in 'minimal';
'off' never warns). Pure; returns a structured verdict, no user-facing strings. An unknown or
non-positive `n` yields `n: null` and no warning.

**Parameters**

- `args` `{enabledKeys: string[], n: number, mode?: 'normal'|'minimal'|'off'}` — enabledKeys — the attribute keys the participant is about to share n — approximate cohort size for this round/charter mode — 'normal' (combo-space > n) · 'minimal' (near-certain: combo-space > n×MINIMAL_FACTOR) · 'off'

**Returns:** `{warn: boolean, comboSpace: number, enabledCount: number, n: number|null}` — `n` is echoed back, or `null` when the given cohort size was missing/non-finite/non-positive.

## `src/disclosureProfile.js`

### `createDisclosureProfile`

**Kind:** function · **Import:** `createDisclosureProfile` from `'@onderling/attribute-charter'`

```js
createDisclosureProfile({ projectId, charterVersion = 1 } = {})
```

A fresh, empty (share-nothing) profile for a project.

**Parameters**

- `args` `{projectId: string, charterVersion?: number}`

### `setValue`

**Kind:** function · **Import:** `setValue` from `'@onderling/attribute-charter'`

```js
setValue(profile, key, value)
```

Set the coarse value for an attribute (does NOT enable sharing it — that is a
separate, deliberate opt-in). Returns a new profile; rejects fine/unknown values.

### `setEnabled`

**Kind:** function · **Import:** `setEnabled` from `'@onderling/attribute-charter'`

```js
setEnabled(profile, key, on)
```

Enable/disable sharing an attribute. Default (absent) = withheld. Enabling a
key with no value set is a no-op on release (nothing to share) but is allowed
so the UI can toggle before entering a value.

### `enabledSharedKeys`

**Kind:** function · **Import:** `enabledSharedKeys` from `'@onderling/attribute-charter'`

```js
enabledSharedKeys(profile, charter)
```

The keys this profile is currently set to share (enabled AND has a valid value).

### `releasedValues`

**Kind:** function · **Import:** `releasedValues` from `'@onderling/attribute-charter'`

```js
releasedValues(profile, charter)
```

The values that will actually be released for this charter: only keys that are
(a) requested by the charter, (b) enabled, and (c) hold a valid coarse value.
Withheld keys are simply ABSENT — no marker. Returns a plain `{key: value}`.

## `src/kAnon.js`

### `attributeKDefault`

**Kind:** function · **Import:** `attributeKDefault` from `'@onderling/attribute-charter'`

```js
attributeKDefault(aggregationK)
```

Default attributeK: attributes are more identifying than mere participation,
so the floor is higher than the aggregation's own `k`.

**Parameters**

- `[aggregationK]` `number` — the project's existing aggregation k (default 3)

### `suppressRareAttributes`

**Kind:** function · **Import:** `suppressRareAttributes` from `'@onderling/attribute-charter'`

```js
suppressRareAttributes(records, { attributeK } = {})
```

Suppress the attributes of every record whose exact attribute-combo is held by
fewer than `attributeK` records. Returns NEW records with `attributes` replaced
by `{}` when suppressed (absent, not marked). Records with no attributes are
left untouched (their empty combo is common and carries nothing to hide).

**Parameters**

- `records` `Array<{attributes?: object}>`
- `opts` `{attributeK: number}`

**Returns:** `Array<{attributes: object}>` — same order; suppressed records have attributes: {}

## `src/vocabulary.js`

### `VOCABULARY`

**Kind:** constant · **Import:** `VOCABULARY` from `'@onderling/attribute-charter'`

Curated coarse vocabulary — the ONLY attributes a charter can request, at the
ONLY granularities allowed. The coarsening is part of the vocabulary itself:
a project lead literally cannot request "exact age" or an address, because the
value space is fixed to a handful of buckets here. New attributes are added
centrally (with a coarsening), never invented per-project.

See plans/NOTE-requested-attributes-charter.md §2. Sits inside the general
personal-properties / disclosure model (NOTE-personal-personal-and-disclosure).

### `attributeKeys`

**Kind:** function · **Import:** `attributeKeys` from `'@onderling/attribute-charter'`

```js
attributeKeys()
```

The attribute keys the vocabulary offers.

### `isVocabKey`

**Kind:** function · **Import:** `isVocabKey` from `'@onderling/attribute-charter'`

```js
isVocabKey(key)
```

Is `key` a known vocabulary attribute?

### `bucketsFor`

**Kind:** function · **Import:** `bucketsFor` from `'@onderling/attribute-charter'`

```js
bucketsFor(key)
```

The allowed buckets for an enum attribute, or null for an open-coarse one (place).

### `isValidValue`

**Kind:** function · **Import:** `isValidValue` from `'@onderling/attribute-charter'`

```js
isValidValue(key, value)
```

Is `value` an allowed coarse value for `key`?
- enum attributes: must be exactly one of the buckets.
- `place` (open-coarse): a short non-empty label that is not a number /
  coordinate / postcode-looking string (defence-in-depth against fine values
  sneaking through; the vocabulary is the primary guard).

### `bucketCount`

**Kind:** function · **Import:** `bucketCount` from `'@onderling/attribute-charter'`

```js
bucketCount(key)
```

The maximum bucket count for an attribute — used by the device-warning combo-space estimate.

### `PLACE_COMBO_WEIGHT`

**Kind:** constant · **Import:** `PLACE_COMBO_WEIGHT` from `'@onderling/attribute-charter'`

Effective bucket count `bucketCount` assigns the open-coarse `place` attribute (no fixed buckets).

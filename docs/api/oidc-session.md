# `@onderling/oidc-session` — API reference

> Generated by `node scripts/api-appendix.mjs` from the exported surface and source JSDoc.
> Do not edit by hand — edit the JSDoc in the source and regenerate. `npm run api-fitness` fails on drift.

Solid OIDC session manager for Node — SolidVault. Peer of @onderling/oidc-session-rn (the React Native variant). Extracted from @onderling/core/storage 2026-05-11 as part of the standardisation P1 work (Phase 50.1).

README: [`packages/oidc-session/README.md`](../../packages/oidc-session/README.md) · Index: [docs/api/README.md](README.md)

## `src/SolidVault.js`

### `_setSessionFactory`

**Kind:** function · **Import:** `_setSessionFactory` from `'@onderling/oidc-session'`

```js
_setSessionFactory(factory)
```

Test-only seam: replace the Session constructor with a fake.
Pass `null` to restore the default.

**Parameters**

- `factory` `(opts: object) => Promise<object>|object`

### `SolidVault`

**Kind:** class · **Import:** `SolidVault` from `'@onderling/oidc-session'`

```js
class SolidVault extends EventEmitter
new SolidVault({ webid, oidcIssuer, redirectUrl, vault } = {})
```

Solid OIDC session manager for Node — despite the name, not itself a `Vault`; it persists its
tokens into a Vault-shaped store under `solid-oidc:<webid>:*` (refresh token in the vault,
access token in memory only). Delegates the OIDC flow to `@inrupt/solid-client-authn-node`'s
`Session`; `getAuthenticatedFetch()` transparently refreshes when the access token is within
60s of expiry, coalescing concurrent refreshes onto one in-flight promise. Emits `'auth-state'`
with `'authenticated'` | `'unauthenticated'` | `'refreshed'` | `'expired'`.

**Methods:** `getPodRoot()` · `isAuthenticated()` · `login()` · `logout()` · `refresh()` · `getAuthenticatedFetch()`

## `src/createSolidAuthNode.js`

### `OIDC_VAULT_KEYS`

**Kind:** constant · **Import:** `OIDC_VAULT_KEYS` from `'@onderling/oidc-session'`

Frozen map of the vault key names under which the Node Solid-OIDC auth persists its state:
refresh token, issuer, client id, and client secret.

### `_setSolidAuthNodeSessionFactory`

**Kind:** function · **Import:** `_setSolidAuthNodeSessionFactory` from `'@onderling/oidc-session'`

```js
_setSolidAuthNodeSessionFactory(factory)
```

Test-only seam. Pass `null` to restore the default.

### `createSolidAuthNode`

**Kind:** function · **Import:** `createSolidAuthNode` from `'@onderling/oidc-session'`

```js
createSolidAuthNode({ vault, clientName } = {})
```

Build a `SolidAuth`-shaped instance bound to a vault.

**Parameters**

- `opts` `object`
- `opts.vault` `object` — implements `get(key)`, `set(key, value)`, `delete(key)`
- `opts.clientName` `string` — display name shown on the OIDC consent screen

**Returns:** `SolidAuth`

## `src/issuerPickerHtml.js`

### `getIssuerPickerHtml`

**Kind:** function · **Import:** `getIssuerPickerHtml` from `'@onderling/oidc-session'`

```js
getIssuerPickerHtml({ selectedId = DEFAULT_ISSUER_ID, customAllowed = true, namePrefix = '', customUrl = '', legend = 'Pod provider', customLabel = 'Custom URL', customPlaceholder = DEFAULT_PLACEHOLDER, } = {})
```

Render the Solid OIDC issuer picker as a server-side HTML fragment: a `<fieldset>` of radio
options for the known issuers plus an optional custom-URL row. Semantic markup only — no inline
styles, no scripts; form names are scoped via `namePrefix` so multiple pickers can coexist.
All options are documented in the file-header JSDoc above.

**Returns:** `string` — the HTML fragment

## `src/issuers.js`

### `KNOWN_ISSUERS`

**Kind:** constant · **Import:** `KNOWN_ISSUERS` from `'@onderling/oidc-session'`

@type {Readonly<KnownIssuer[]>}

### `DEFAULT_ISSUER_ID`

**Kind:** constant · **Import:** `DEFAULT_ISSUER_ID` from `'@onderling/oidc-session'`

Issuer id used when the caller doesn't pick one.

### `DEFAULT_ISSUER`

**Kind:** constant · **Import:** `DEFAULT_ISSUER` from `'@onderling/oidc-session'`

Convenience — the resolved default.

### `resolveIssuer`

**Kind:** function · **Import:** `resolveIssuer` from `'@onderling/oidc-session'`

```js
resolveIssuer(idOrUrl)
```

Resolve a `KnownIssuer` from either an id (`'inrupt'`,
`'solidcommunity'`, …) or a full URL. URLs that don't match any
known issuer fall back to a synthesised `{ id: 'custom', url, label,
capabilities: { dcr: 'unknown', acp: 'unknown', dpop: 'unknown' } }`
so callers can still construct sessions against arbitrary
spec-compliant servers (self-hosted CSS, less common community
providers).

Returns `null` on malformed input (not a known id and not a valid
URL).

**Parameters**

- `idOrUrl` `string`

**Returns:** `KnownIssuer | null`

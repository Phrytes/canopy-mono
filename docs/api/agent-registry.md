# `@onderling/agent-registry` — API reference

> Generated by `node scripts/api-appendix.mjs` from the exported surface and source JSDoc.
> Do not edit by hand — edit the JSDoc in the source and regenerate. `npm run api-fitness` fails on drift.

Canonical agent registry — the user's agents listed in one pod resource. Implements core's ActorResolver. Etag-based optimistic concurrency.

README: [`packages/agent-registry/README.md`](../../packages/agent-registry/README.md) · Index: [docs/api/README.md](README.md)

**Entry points**

- `'@onderling/agent-registry'`
- `'@onderling/agent-registry/offerings-taxonomy'`
- `'@onderling/agent-registry/src/offeringsTaxonomy.js'`

## `src/AgentRegistry.js`

### `createAgentRegistry`

**Kind:** function · **Import:** `createAgentRegistry` from `'@onderling/agent-registry'`

```js
createAgentRegistry({ pseudoPod, anchorPodUri, deviceId, preferPodUri = false, resourceUri, maxRetries, onPersistentConflict, // identity step 2.5 — optional @onderling/versioning store. When supplied, every registry write // snapshots the resource, so the profile set gets history / undoable recovery (the registry // lives on the pseudo-pod, which is NOT a versioned circle pod, so it gets none for free). versionStore = null, now = () => new Date().toISOString(), } = {})
```

Build the live agent-registry handle over an injected pseudo-pod: etag-CAS reads/writes of the
registry resource with retry, plus optional per-write snapshots via a `versionStore`. Returns
`{register, lookup, revoke, purge, updateCapabilities, applyGrant, revokeGrant, list, reload,
resourceUri}`. The resource URI resolves from the `resourceUri` override, else `deviceId`
(pseudo-pod path), else `anchorPodUri`; throws INVALID_ARGUMENT without a usable `pseudoPod`.

**Parameters**

- `opts` `object`
- `opts.pseudoPod` `object`
- `[opts.anchorPodUri]` `string` — pod URI (metadata; pod-side mirroring lands with cache-mode pseudo-pod).
- `[opts.deviceId]` `string` — strongly recommended; default store path.
- `[opts.preferPodUri]` `boolean` — force the https:// resource path (caller's pseudo-pod must accept it).
- `[opts.resourceUri]` `string` — explicit override (wins over all defaults).
- `[opts.maxRetries=3]` `number`
- `[opts.onPersistentConflict]` `(err: Error) => void`
- `[opts.versionStore]` `object` — optional @onderling/versioning store; when supplied, every write snapshots the resource (best-effort).
- `[opts.now]` `() => string`

## `src/agentCard.js`

### `projectAgentCard`

**Kind:** function · **Import:** `projectAgentCard` from `'@onderling/agent-registry'`

```js
projectAgentCard(entry, { owner } = {})
```

Project a registry agent entry to a frozen A2A Agent Card: the standard card fields plus the
`x-canopy` extension block (ownership, grants, lifecycle status). Skill ids are the sorted,
de-duplicated union of grant skills and coarse capabilities. Throws INVALID_ARGUMENT when
`entry` / `entry.agentId` is missing.

**Parameters**

- `entry` `object` — a registry agent entry (v2 shape)
- `[opts]` `object`
- `[opts.owner]` `string` — owner webid/key; defaults to entry.webid

**Returns:** `object` — frozen A2A agent card

## `src/availability.js`

### `AVAILABILITY_STATES`

**Kind:** constant · **Import:** `AVAILABILITY_STATES` from `'@onderling/agent-registry'`

The valid availability states, coarsest concept first (frozen).

### `AVAILABILITY_AWAY`

**Kind:** constant · **Import:** `AVAILABILITY_AWAY` from `'@onderling/agent-registry'`

The coarse 'away' value — this IS holiday mode (the old `holidayMode:true`).

### `AVAILABILITY_LADDER`

**Kind:** constant · **Import:** `AVAILABILITY_LADDER` from `'@onderling/agent-registry'`

The availability coarseness ladder, COARSEST→finest (propertyVocabulary convention):
`state` (just the state string) → `detail` (the whole value incl. a future free-text
"when"). Below `state` sits the implicit ∅ — disclosure withholds the key entirely.

### `isAvailabilityState`

**Kind:** function · **Import:** `isAvailabilityState` from `'@onderling/agent-registry'`

```js
isAvailabilityState(v)
```

True iff `v` is one of the AVAILABILITY_STATES strings.

### `availabilityState`

**Kind:** function · **Import:** `availabilityState` from `'@onderling/agent-registry'`

```js
availabilityState(value)
```

Extract the coarse STATE from a stored availability value. Accepts both the plain
string form (`'away'`) and a future structured form (`{ state, when }`) — returns the
state string, or `null` when nothing recognisable is present.

**Parameters**

- `value` `string|{state?:string}`

**Returns:** `string|null`

### `isAway`

**Kind:** function · **Import:** `isAway` from `'@onderling/agent-registry'`

```js
isAway(value)
```

True iff the availability value resolves to 'away' (holiday / not reachable).

### `availabilityDescriptor`

**Kind:** function · **Import:** `availabilityDescriptor` from `'@onderling/agent-registry'`

```js
availabilityDescriptor(key = 'availability')
```

The property-vocabulary descriptor for the availability key. Type `coarse-enum`
(a small bucket set, like place/ageBand), ladder `state → detail`. Coarsening is
FAIL-CLOSED: only the `detail` rung releases the whole value; `state` — and any
null/unknown rung — collapses to the bare state string (dropping any free-text
"when", a future 'detail'-only field).

**Parameters**

- `[key='availability']` `string`

## `src/catalogSource.js`

### `createCatalogSource`

**Kind:** function · **Import:** `createCatalogSource` from `'@onderling/agent-registry'`

```js
createCatalogSource({ resolveEndorsements, endorsementResource, roots, resolveCard, maxDepth = DEFAULT_MAX_DEPTH, cache, now, } = {})
```

createCatalogSource — a `{ list, get }` source over the endorsement graph.

**Parameters**

- `opts` `object`
- `[opts.resolveEndorsements]` `(endorser: string) => (object[]|Promise<object[]>)` — per-curator endorsement lists (the general web-of-trust seam).
- `[opts.endorsementResource]` `{ list: () => Promise<object[]> }` — G1 back-compat: one shared endorsement pool. Ignored if `resolveEndorsements` is given.
- `opts.roots` `string[]|string|(() => (string[]|Promise<string[]>))` — curator root pubKey(s) (depth 0). A THUNK is resolved on every derive, so a live subscription set (subscribe/unsubscribe) takes effect immediately (commons-governance G3 — subscribed-community admins are the roots).
- `opts.resolveCard` `(subject: string, endorsement: object) => (object|null|Promise<object|null>)` — resolves an endorsed agent's Agent Card by its subject pubKey.
- `[opts.maxDepth=DEFAULT_MAX_DEPTH]` `number` — bounded walk depth.
- `[opts.cache]` `{ read: () => (object[]|null), write: (entries: object[]) => void }` — optional offline read-through cache.
- `[opts.now]` `() => number` — injectable clock (expiry; tests)

**Returns:** `{ list: () => Promise<object[]>, get: (id: string) => Promise<object|null> }`

## `src/communityCatalog.js`

### `communityCatalogUri`

**Kind:** function · **Import:** `communityCatalogUri` from `'@onderling/agent-registry'`

```js
communityCatalogUri({ circleId, anchorPodUri, deviceId, preferPodUri = false } = {})
```

Circle-scoped shared-readable resource URI for a community catalog. Distinct
from the per-endorser G1 path (`/public/endorsements`) so a device can host
both its own endorsements AND a community it admins, keyed by circleId.

### `createCommunityCatalog`

**Kind:** function · **Import:** `createCommunityCatalog` from `'@onderling/agent-registry'`

```js
createCommunityCatalog({ circleId, isAdmin, pseudoPod, anchorPodUri, deviceId, preferPodUri = false, resourceUri, maxRetries, onPersistentConflict, ensureAccess, now, } = {})
```

createCommunityCatalog — an admin-gated, circle-owned endorsement resource.

Writes (`endorse`, `revoke`, `fork`) are gated to the circle's admins via the
injected `isAdmin` predicate; reads (`list`, `get`) are open (the resource is
shared-readable, authority = the signature). Built ON TOP of the G1
`createEndorsementResource` (etag-CAS append/revoke reused unchanged).

**Parameters**

- `opts` `object`
- `opts.circleId` `string` — the owning circle's id.
- `opts.isAdmin` `(endorserPubKey: string) => (boolean|Promise<boolean>)` — the CIRCLE's admin/policy gate (reuse circles' `inAudience('role:admin')`).
- `opts.pseudoPod` `object` — injected pod I/O (same as the G1 resource).
- `[opts.anchorPodUri]` `string`
- `[opts.deviceId]` `string` — default store path (strongly recommended).
- `[opts.preferPodUri]` `boolean`
- `[opts.resourceUri]` `string` — explicit override (wins).
- `[opts.maxRetries]` `number`
- `[opts.onPersistentConflict]` `(err: Error) => void`
- `[opts.now]` `() => string`
- `[opts.ensureAccess]` `(uri: string) => (any|Promise<any>)` — best-effort real-pod access-control hook, forwarded to the underlying endorsement resource. For a community catalog the app wires it to `setResourceAccess` with **public-read + owner-write + admin-write** (the circle's admins' WebIDs — resolve admin pubKeys→WebIDs via the identity resolver / `AgentRegistryMemberMap`; in basis webid===pubKey today). Hermetic no-op on the pseudo-pod. // G3-seam: admin WebIDs via MemberMap.

**Returns:** `{ endorse, revoke, fork, list, get, ensureAccess, circleId: string, resourceUri: string }`

## `src/concurrency.js`

### `withCAS`

**Kind:** function · **Import:** `withCAS` from `'@onderling/agent-registry'`

```js
async withCAS({ readCurrent, mutate, writeNext, maxRetries = DEFAULT_MAX_RETRIES, backoffMs = DEFAULT_BACKOFF_MS, onPersistentConflict, sleep, } = {})
```

Run one read → mutate → write cycle under etag compare-and-swap, retrying on `CONFLICT` with a
short backoff (10/50/200 ms) up to `maxRetries` (default 3). Mutation errors propagate verbatim;
after retries are exhausted, `onPersistentConflict` fires and a `PERSISTENT_CONFLICT` error is
thrown.

**Parameters**

- `args` `object`
- `args.readCurrent` `() => Promise<{body: object, etag?: string | null}>`
- `args.mutate` `(current: object, etag: string | null) => Promise<object> | object` — returns the new body. May throw to abort the mutation.
- `args.writeNext` `(body: object, etag: string | null) => Promise<{etag?: string}>` — returns the new etag. Throws `{code: 'CONFLICT'}` on 412.
- `[args.maxRetries=3]` `number`
- `[args.backoffMs=[10, 50, 200]` `number[]` — ] — per-attempt retry delays; the last entry repeats when attempts outnumber entries.
- `[args.onPersistentConflict]` `(error: Error) => void`
- `[args.sleep]` `(ms: number) => Promise<void>`

**Returns:** `Promise<{body: object, etag?: string, retries: number}>`

## `src/createProfile.js`

### `createProfile`

**Kind:** function · **Import:** `createProfile` from `'@onderling/agent-registry'`

```js
async createProfile({ registry, ownerRoot, profileId, role = 'profile', name = null, properties = {}, agentUri } = {})
```

Create (register) a root-derived PROFILE entry in the registry (identity step 2). The profile's
signing key is HKDF-derived from the owner root + `profileId`, so the same inputs reproduce the
same pubKey on any device; only the pubKey is recorded — the full identity is re-derived on the
device that actually runs the profile.

**Parameters**

- `a` `object`
- `a.registry` `object` — a createAgentRegistry handle
- `a.ownerRoot` `object` — a core Bootstrap (deriveAgentSeed + fingerprint)
- `a.profileId` `string` — stable per-profile label (also the registry agentId)
- `[a.role]` `string` — default 'profile'
- `[a.name]` `string`
- `[a.properties]` `object` — own/inherit property map
- `[a.agentUri]` `string` — default `profile:<id>`

**Returns:** `Promise<{ entry: object, pubKey: string }>`

### `profilePubKey`

**Kind:** function · **Import:** `profilePubKey` from `'@onderling/agent-registry'`

```js
profilePubKey(ownerRoot, profileId)
```

The deterministic pubKey a profile WOULD have (recovery / verification), without registering.

### `profileCircleAddress`

**Kind:** function · **Import:** `profileCircleAddress` from `'@onderling/agent-registry'`

```js
profileCircleAddress(ownerRoot, profileId, circleId)
```

The per-circle ADDRESS a profile presents in a circle (step 3 — unlinkable-by-default). Full chain
from the owner root: root → profile seed → per-circle address. A distinct key per (profile, circle).

## `src/disclosure.js`

### `createDisclosurePolicy`

**Kind:** function · **Import:** `createDisclosurePolicy` from `'@onderling/agent-registry'`

```js
createDisclosurePolicy()
```

A fresh, share-nothing policy.

### `setDisclosure`

**Kind:** function · **Import:** `setDisclosure` from `'@onderling/agent-registry'`

```js
setDisclosure(policy, contextId, key, choice = {})
```

Set the disclosure of one property for one context across the THREE independent axes.
Returns a NEW policy. Default is withhold — you only ever ADD a deliberate share.

Each axis is set INDEPENDENTLY: pass only the axis you want to change and the others are
PRESERVED (not clobbered), so `matchable` can be toggled without touching `enabled`, and vice
versa. An axis never touched stays at its withheld default (false / null). This is what lets
matchable be TRUE while disclosed is FALSE.

**Parameters**

- `policy` `object`
- `contextId` `string`
- `key` `string`
- `[choice]` `{ enabled?: boolean, rung?: string|null, matchable?: boolean, requestable?: boolean }`

### `getDisclosure`

**Kind:** function · **Import:** `getDisclosure` from `'@onderling/agent-registry'`

```js
getDisclosure(policy, contextId, key)
```

The disclosure choice for a key in a context, across all three axes. Default = full withhold
({enabled:false, rung:null, matchable:false, requestable:false}). Backward-compatible: an old
entry carrying only {enabled,rung} reads as matchable:false/requestable:false.

### `isDisclosed`

**Kind:** function · **Import:** `isDisclosed` from `'@onderling/agent-registry'`

```js
isDisclosed(policy, contextId, key)
```

Is this property DISCLOSED (visible to the circle) for this context? (the released axis)

### `isMatchable`

**Kind:** function · **Import:** `isMatchable` from `'@onderling/agent-registry'`

```js
isMatchable(policy, contextId, key)
```

May this property participate in MATCHING for this context — even without being disclosed?

### `isRequestable`

**Kind:** function · **Import:** `isRequestable` from `'@onderling/agent-registry'`

```js
isRequestable(policy, contextId, key)
```

May others' agents invoke/ping about this property for this context? (default false)

### `releasedValues`

**Kind:** function · **Import:** `releasedValues` from `'@onderling/agent-registry'`

```js
releasedValues({ getProfile, profileId, defaultProfileId = null }, request, policy, contextId, vocabulary = null)
```

The values a REQUEST may see for a CONTEXT, given the user's profile + disclosure policy.
For each requested key: if the user enabled it for this context AND the profile resolves a
value (own, or inherited up the profile chain), release that value coarsened to the chosen
rung. Everything else is ABSENT (default-withhold, no marker). Pure.

**Parameters**

- `profileCtx` `object`
- `profileCtx.getProfile` `(id:string)=>({properties?:object}|null|undefined)`
- `profileCtx.profileId` `string`
- `[profileCtx.defaultProfileId]` `string|null`
- `request` `{ items?: Array<{key:string}> }` — the consumer's typed request
- `policy` `object` — the user's disclosure policy
- `contextId` `string`
- `[vocabulary]` `object` — a createVocabulary(...) — for coarsening (optional)

**Returns:** `object` — { [key]: releasedValue } for enabled + resolvable keys only

### `releasedForMatching`

**Kind:** function · **Import:** `releasedForMatching` from `'@onderling/agent-registry'`

```js
releasedForMatching({ getProfile, profileId, defaultProfileId = null }, request, policy, contextId, vocabulary = null)
```

The MATCHING counterpart to `releasedValues` — the values the on-device matcher may consult for a
CONTEXT, keyed off the `matchable` axis (isMatchable), INDEPENDENT of `disclosed`/`enabled`.

This is NOT a disclosure. The output is fed ONLY to the on-device matcher (per the one-sided,
de-identification model: the matched-AGAINST party consents that a property "may be checked for
matches", and a hit in an anonymous circle doesn't reveal WHO — NOTE-skills-vs-capabilities.md
volley 3). It must NEVER be rendered on the roster, added to the released/disclosed set, or used to
open a request channel. `releasedValues` remains the ONLY disclosure surface and stays
`enabled`-only, so a matchable-but-not-disclosed property is surfaced HERE and nowhere else. True
two-sided-blind matching (neither side exposed) is the TEE upgrade, deferred (drivers-#6).

Symmetry with `releasedValues`: same resolve → coarsen → fail-closed pipeline (a value the coarsen
fn can't reduce is WITHHELD from the matcher too — matching never leaks a raw fine value either),
differing ONLY in the axis it keys off (`matchable` here, `enabled` there).

Scoping: with a typed `request` (items[]) it restricts to those keys (symmetry with releasedValues);
with NO request it surfaces EVERY matchable key for the context — the match-PROPOSAL path (a
property change / circle-join runs the matcher with no incoming request).

**Parameters**

- `profileCtx` `object` — { getProfile, profileId, defaultProfileId } (as releasedValues)
- `[request]` `{ items?: Array<{key:string}> }|null` — optional key filter; null = all matchable keys
- `policy` `object` — the user's disclosure policy
- `contextId` `string`
- `[vocabulary]` `object` — a createVocabulary(...) — for coarsening (optional)

**Returns:** `object` — { [key]: matchableValue } for matchable + resolvable keys only

### `REVEAL_PRESETS`

**Kind:** constant · **Import:** `REVEAL_PRESETS` from `'@onderling/agent-registry'`

Reveal presets, LEAST → MOST revealing (by amount). `handle` = floor, `full` = ceiling.

### `isRevealPreset`

**Kind:** function · **Import:** `isRevealPreset` from `'@onderling/agent-registry'`

```js
isRevealPreset(p)
```

True iff `p` is one of the REVEAL_PRESETS (`handle` | `profile` | `full`).

### `revealPresetRank`

**Kind:** function · **Import:** `revealPresetRank` from `'@onderling/agent-registry'`

```js
revealPresetRank(preset)
```

Rank a preset (0=handle … 2=full), or -1 for an unknown preset.

### `nextRevealPreset`

**Kind:** function · **Import:** `nextRevealPreset` from `'@onderling/agent-registry'`

```js
nextRevealPreset(preset)
```

The next preset up (more revealing), or the same when already at the ceiling (`full`).

### `applyRevealPreset`

**Kind:** function · **Import:** `applyRevealPreset` from `'@onderling/agent-registry'`

```js
applyRevealPreset(policy, contextId, preset, { keysFor } = {})
```

Apply a reveal preset to a context: ENABLE every key in tiers ≤ `preset` and DISABLE
every key in tiers > `preset` (so `handle` truly floors — nothing beyond the handle
enabled — and `full` ceilings — all listed keys enabled). Only the `enabled` axis is
touched; `rung`/`matchable`/`requestable` for each key are PRESERVED. Returns a NEW
policy (pure). Per-attribute booleans stay the truth: hide one key afterwards and the
context reads as "this preset minus that key".

**Parameters**

- `policy` `object`
- `contextId` `string`
- `preset` `string` — one of REVEAL_PRESETS
- `opts` `{ keysFor: (preset:string)=>string[] }` — the caller's per-tier key assignment

**Returns:** `object` — a new policy

### `revealPresetOf`

**Kind:** function · **Import:** `revealPresetOf` from `'@onderling/agent-registry'`

```js
revealPresetOf(policy, contextId, { keysFor } = {})
```

The highest preset FULLY satisfied for a context: the most-revealing preset whose whole
cumulative key-set is `enabled`. Because presets are amount-cumulative, once a preset's
set is not fully enabled no higher preset can be either, so the scan stops there. A
`handle` tier with no keys is vacuously satisfied → the floor reads as `handle`. Returns
`null` only when even the `handle` floor's (non-empty) keys are not all enabled.

**Parameters**

- `policy` `object`
- `contextId` `string`
- `opts` `{ keysFor: (preset:string)=>string[] }`

**Returns:** `'handle'|'profile'|'full'|null`

## `src/driverMatch.js`

### `deriveSignature`

**Kind:** function · **Import:** `deriveSignature` from `'@onderling/agent-registry'`

```js
deriveSignature({ text = '', tags = [] } = {})
```

A "driver signature" is what an item carries for matching: `{ text, tags[] }` (#5 puts it on
questions/tasks). Normalises to the same tag space as a driver so overlap is stable.

**Parameters**

- `sig` `{text?:string, tags?:string[]}`

### `itemSignature`

**Kind:** function · **Import:** `itemSignature` from `'@onderling/agent-registry'`

```js
itemSignature(item)
```

Derive a driver signature FROM an item. An item that carries an explicit `driverSignature`
(#5 — questions/tasks author it) uses that; otherwise fall back to the item's own `{text, tags}`
(so a plain tagged post still matches). Returns a normalised `{text, tags}`.

**Parameters**

- `item` `{driverSignature?:object, text?:string, title?:string, tags?:string[]}`

### `matchProfileDrivers`

**Kind:** function · **Import:** `matchProfileDrivers` from `'@onderling/agent-registry'`

```js
matchProfileDrivers({ properties, item, judge, minShared = 1 } = {})
```

Convenience bridge: match an item against the DRIVER properties held on a profile's property map
(pulls the drivers out with `driversFromProperties`, derives the item signature, then matches).
Deterministic when no `judge`; semantic when one is wired. This is the seam the app calls per
incoming feed item — the private drivers stay in the profile, on-device.

**Parameters**

- `a` `object`
- `a.properties` `Record<string, any>` — the profile's full property map
- `a.item` `object` — the incoming item (carries a driverSignature or text/tags)
- `[a.judge]` `Function` — optional injected LLM judge
- `[a.minShared=1]` `number`

**Returns:** `Promise<Array<object>>`

### `matchableSignature`

**Kind:** function · **Import:** `matchableSignature` from `'@onderling/agent-registry'`

```js
matchableSignature(matchableValues)
```

Build a driver-matching SIGNATURE from a candidate's MATCHABLE value-set — the `{ key → value }`
map produced by `releasedForMatching` (disclosure.js). This is the counterpart to `itemSignature`:
where that turns an incoming feed ITEM into a signature, this turns another person's matchable
surface into one, so MY drivers can be scored against it (profile↔profile matching).

A driver value contributes its tags (+ text); a plain string value (a coarse-enum like a place)
contributes as a single tag. Everything flattens into the SAME normalised tag space the item
matcher already uses, so no new matching logic is needed — just a new signature source.

NOTE: the input is a MATCHABLE set, never a disclosed one — the caller feeds it the output of
`releasedForMatching`, NOT `releasedValues`. The values are used only to compute a match on the
matcher's device; they are not surfaced anywhere (the de-identification model).

**Parameters**

- `matchableValues` `Record<string, any>` — releasedForMatching(...) output

**Returns:** `{text:string, tags:string[]}`

### `matchProfilesMatchable`

**Kind:** function · **Import:** `matchProfilesMatchable` from `'@onderling/agent-registry'`

```js
matchProfilesMatchable({ properties, candidateMatchable, judge, minShared = 1 } = {})
```

PROFILE↔PROFILE matchable-aware match: score MY private drivers against a CANDIDATE's MATCHABLE
surface. My side never leaves my device (the drivers stay in `properties`); the candidate's side is
ONLY the matchable set they consented to expose for matching (`releasedForMatching` output) — never
their disclosed/roster values. Reuses the exact same explainable matcher (`matchDriversSemantic`):
tag overlap, optional injected judge, every surfaced match carries its plain-language reason.

This is the on-device matcher's matchable path. The existing item path (`matchProfileDrivers`) is
unchanged — this ADDS the profile↔profile path by feeding the matcher a matchable-derived signature
instead of an item-derived one.

**Parameters**

- `a` `object`
- `a.properties` `Record<string, any>` — MY full property map (drivers pulled out locally)
- `a.candidateMatchable` `Record<string, any>` — a candidate's releasedForMatching(...) value-set
- `[a.judge]` `Function` — optional injected LLM judge
- `[a.minShared=1]` `number`

**Returns:** `Promise<Array<object>>`

### `sharedTags`

**Kind:** function · **Import:** `sharedTags` from `'@onderling/agent-registry'`

```js
sharedTags(a = [], b = [])
```

The tags two tag-lists share (normalised inputs assumed), first-seen order of `a`.

### `jaccard`

**Kind:** function · **Import:** `jaccard` from `'@onderling/agent-registry'`

```js
jaccard(a = [], b = [])
```

Jaccard overlap ∈ [0,1] over two tag sets — used ONLY to RANK explainable matches, never to gate.

### `scoreDriver`

**Kind:** function · **Import:** `scoreDriver` from `'@onderling/agent-registry'`

```js
scoreDriver(driver, signature, { minShared = 1 } = {})
```

Deterministic score of ONE driver against a signature. Returns a match ONLY when they share at
least `minShared` tags (the explainable signal); otherwise null. `score` (Jaccard) is for ranking.

**Parameters**

- `driver` `{kind:string, text:string, tags:string[]}`
- `signature` `{text:string, tags:string[]}`
- `[opts]` `{minShared?:number}`

**Returns:** `{kind:string, score:number, sharedTags:string[], reason:{kind:'tags',tags:string[]}}|null`

### `matchDrivers`

**Kind:** function · **Import:** `matchDrivers` from `'@onderling/agent-registry'`

```js
matchDrivers({ drivers, signature, minShared = 1 } = {})
```

DETERMINISTIC on-device match: score every private driver against the item signature, keep the
explainable (tag-overlapping) ones, ranked by score desc. `drivers` is a `{ key → driverValue }`
map (as stored on the profile) OR an array of driver values.

**Parameters**

- `a` `object`
- `a.drivers` `Record<string,object>|Array<object>`
- `a.signature` `{text?:string, tags?:string[]}`
- `[a.minShared=1]` `number`

**Returns:** `Array<{key:string|null, kind:string, score:number, sharedTags:string[], reason:object}>`

### `matchDriversSemantic`

**Kind:** function · **Import:** `matchDriversSemantic` from `'@onderling/agent-registry'`

```js
async matchDriversSemantic({ drivers, signature, judge, minShared = 1 } = {})
```

SEMANTIC on-device match: the deterministic matches PLUS, when an LLM `judge` is wired, the drivers
that DON'T share tags but the judge rules resonant (catching the synonyms tag-overlap misses). The
judge runs locally/privatemode/companion — the drivers still never leave a trusted environment.
Every judge match carries the judge's own reason, so the explainability invariant holds.

`judge({ driver, signature })` resolves `{ match:boolean, reason?:string }`. A judge that throws (or
a missing judge) simply yields no semantic additions — the deterministic layer always stands.

**Parameters**

- `a` `object`
- `a.drivers` `Record<string,object>|Array<object>`
- `a.signature` `{text?:string, tags?:string[]}`
- `[a.judge]` `(x:{driver:object, signature:object})=>Promise<{match:boolean, reason?:string}>`
- `[a.minShared=1]` `number`

**Returns:** `Promise<Array<{key:string|null, kind:string, score:number, sharedTags:string[], reason:object}>>`

## `src/drivers.js`

### `DRIVER_KINDS`

**Kind:** constant · **Import:** `DRIVER_KINDS` from `'@onderling/agent-registry'`

The kinds of personal driver. `driver` is the generic catch-all; the rest are finer intents.
`offering` (offering→property fold-in, NOTE-skills-properties-audit) is a driver-LIKE open
item — what someone can DO/offer rather than wants — sharing the `{ kind, text, tags[] }` shape;
its coarse rung (categoryId under disclosure) lives in `offeringsTaxonomy.js`, not here.

`interest` (interests→drivers fold-in, NOTE-skills-properties-audit 4) is likewise a
driver-kind open item — what someone ENGAGES WITH / cares about — carried as tags on the
`{ kind, text, tags[] }` shape (no taxonomy / coarse rung: interests are FREE drivers, matched
on-device by tag overlap). It folds the bespoke learned Layer-2 interest signal
(stoop InterestProfile top terms) into one disclosure-controlled property on the profile graph.

Legacy `skill` (the pre-rename name for the human "I can do X" DATA — see
NOTE-offering-rename-inventory.md) is READ-ACCEPTED and normalized to `offering`; new writes use
`offering`. The INVOCABLE A2A sense keeps the word "skill" elsewhere and is unrelated here.

### `normalizeDriverKind`

**Kind:** function · **Import:** `normalizeDriverKind` from `'@onderling/agent-registry'`

```js
normalizeDriverKind(k)
```

Normalize a stored/legacy driver-kind string to its current name.

### `isDriverKind`

**Kind:** function · **Import:** `isDriverKind` from `'@onderling/agent-registry'`

```js
isDriverKind(k)
```

True iff `k` is a DRIVER_KINDS string (or a read-accepted legacy alias, e.g. `skill`).

### `normalizeTag`

**Kind:** function · **Import:** `normalizeTag` from `'@onderling/agent-registry'`

```js
normalizeTag(t)
```

Normalise one tag: lowercased, trimmed, internal whitespace/underscores → single hyphens,
stripped of anything but [a-z0-9-]. Returns '' for junk (callers drop empties). Keeps the tag
vocabulary clean so tag-overlap matching is stable across authors.

### `normalizeTags`

**Kind:** function · **Import:** `normalizeTags` from `'@onderling/agent-registry'`

```js
normalizeTags(tags)
```

Normalise + de-duplicate a tag list, preserving first-seen order.

### `createDriver`

**Kind:** function · **Import:** `createDriver` from `'@onderling/agent-registry'`

```js
createDriver({ kind = 'driver', text = '', tags = [], categoryId } = {})
```

Build a validated, frozen driver value: `{ kind, text, tags[] }` (+ `categoryId` for offerings).
- `kind` falls back to the generic `driver` when unknown.
- `text` is a trimmed human phrase (may be empty when tags carry the meaning).
- `tags` are normalised + de-duped.
- `categoryId` (offering kind only): the user-picked taxonomy bucket — the coarse
  rung `offeringDescriptor`'s coarsen() honours over derivation. Dropped for
  other kinds (they have no coarse rung).
- legacy `kind:'skill'` is normalized to `offering` (read-compat).
A driver with neither text NOR tags is meaningless — throws (nothing to disclose or match on).

**Parameters**

- `d` `{kind?:string, text?:string, tags?:string[], categoryId?:string}`

**Returns:** `{kind:string, text:string, tags:string[], categoryId?:string}`

### `isDriverValue`

**Kind:** function · **Import:** `isDriverValue` from `'@onderling/agent-registry'`

```js
isDriverValue(v)
```

Shape guard for an opaque stored property value — true iff `v` is a well-formed driver value.
The property store keeps values opaque, so consumers use this to know "is this key a driver?".

### `driverDescriptor`

**Kind:** function · **Import:** `driverDescriptor` from `'@onderling/agent-registry'`

```js
driverDescriptor(key)
```

A property-vocabulary descriptor for a driver key. Type `driver`, `sensitive` (drivers are
personal), and NO coarseness ladder — a driver is all-or-nothing per context (you share the
whole driver or none of it; there is no "coarser" driver), unlike a coarse-enum attribute.

**Parameters**

- `key` `string`

### `driversFromProperties`

**Kind:** function · **Import:** `driversFromProperties` from `'@onderling/agent-registry'`

```js
driversFromProperties(properties)
```

Extract the DRIVER properties out of a full profile property map — a profile's `properties` holds
every kind (coarse-enum place/ageBand alongside drivers), so the matcher needs just the drivers.
Returns a `{ key → driverValue }` map, keeping only well-formed driver values (isDriverValue).

**Parameters**

- `properties` `Record<string, any>`

**Returns:** `Record<string, {kind:string,text:string,tags:string[]}>`

## `src/endorsement.js`

### `ENDORSEMENT_VERSION`

**Kind:** constant · **Import:** `ENDORSEMENT_VERSION` from `'@onderling/agent-registry'`

Wire-format version stamped (as `v`) on newly created endorsement records.

### `cardHash`

**Kind:** function · **Import:** `cardHash` from `'@onderling/agent-registry'`

```js
cardHash(card)
```

cardHash — `sha256:<b64url>` over the deep-canonical Agent Card.
The one hash function both issue and verify agree on.

### `issueEndorsement`

**Kind:** function · **Import:** `issueEndorsement` from `'@onderling/agent-registry'`

```js
issueEndorsement(endorserIdentity, opts = {})
```

issueEndorsement — sign a `recommend`/`flag` claim over an Agent Card.

**Parameters**

- `endorserIdentity` `import('@onderling/core').AgentIdentity` — signs the claim
- `opts` `object`
- `opts.card` `object` — the Agent Card being endorsed (REQUIRED — cardHash binds to it)
- `[opts.subject]` `string` — endorsed agent pubKey (defaults to the card's pubKey)
- `[opts.claim='recommend']` `'recommend'|'flag'`
- `[opts.tags=[]` `string[]` — ]
- `[opts.note='']` `string`
- `[opts.expiresIn=null]` `number|null` — ms from now; null = no expiry
- `[opts.now]` `() => number` — injectable clock (tests)

**Returns:** `object` — the signed endorsement record

### `verifyEndorsement`

**Kind:** function · **Import:** `verifyEndorsement` from `'@onderling/agent-registry'`

```js
verifyEndorsement(rec, card, { now } = {})
```

verifyEndorsement — deny-by-default validation of a claim against a card.

Valid iff ALL hold:
  1. well-formed record + known claim,
  2. NOT expired (expiresAt in the past → invalid; curation goes stale),
  3. `subject` === the card's declared pubKey,
  4. `cardHash` === `cardHash(card)` — the anti-escalation binding: an agent
     that mutated its card after endorsement no longer matches,
  5. the Ed25519 signature verifies against `endorser` (AgentIdentity.verify).

Any throw / mismatch → returns `false` (never an exception). On success
returns the verified "actor" view `{ endorser, subject, claim, cardHash,
tags, note, issuedAt, expiresAt }` — truthy, and carrying `claim` so the
catalog can separate `recommend` from `flag`.

**Parameters**

- `rec` `object` — an endorsement record
- `card` `object` — the Agent Card the endorsement's subject resolves to
- `[o]` `object`
- `[o.now]` `() => number` — injectable clock (tests)

**Returns:** `(false | { endorser, subject, claim, cardHash, tags, note, issuedAt, expiresAt })`

## `src/endorsementResource.js`

### `ENDORSEMENT_RESOURCE_VERSION`

**Kind:** constant · **Import:** `ENDORSEMENT_RESOURCE_VERSION` from `'@onderling/agent-registry'`

Wire-format version stamped on endorsement-list resources (and defaulted in on read).

### `endorsementResourceUri`

**Kind:** function · **Import:** `endorsementResourceUri` from `'@onderling/agent-registry'`

```js
endorsementResourceUri({ anchorPodUri, deviceId, preferPodUri = false } = {})
```

Shared-readable endorsement-list path for an endorser's pod / device.

Lives under `/public/` (contrast the registry's `/private/`) because the
catalog read-path reads it cross-pod. The pseudo-pod path is the default,
same as the registry.

// real-pod: public-read ACP — on a real Solid pod this resource carries a
// public-READ / owner-WRITE access posture so any client can resolve+verify
// it while only the endorser can publish. That posture is now SET, not just
// marked: an injected best-effort `ensureAccess(uri)` hook (wired by the app
// to `@onderling/pod-client`'s `setResourceAccess` → public-read + owner-write)
// fires once after the first write to a real (https) pod URI, and is exposed
// as `ensureAccess()` for explicit/idempotent re-application. On the
// pseudo-pod (hermetic) the hook never fires — this stays a pure no-op and
// the read+verify path is unchanged. Proven live vs a WAC pod in
// `@onderling/pod-client`'s `setResourceAccess.css.test.js`; ACP-on-CSS is the
// pre-existing Inrupt-SDK interop gap (surfaced, not silently swallowed).

### `emptyEndorsementResource`

**Kind:** function · **Import:** `emptyEndorsementResource` from `'@onderling/agent-registry'`

```js
emptyEndorsementResource()
```

A frozen, empty endorsement-list body: current version, no endorsements, `updatedAt` = now.

### `normaliseEndorsementResource`

**Kind:** function · **Import:** `normaliseEndorsementResource` from `'@onderling/agent-registry'`

```js
normaliseEndorsementResource(raw)
```

Normalise a read resource. Light-touch: authority is the signature (verified
downstream by verifyEndorsement), so this only guards the LIST shape and
drops entries with no usable `id` — it does NOT re-sign or trust the fields.

### `createEndorsementResource`

**Kind:** function · **Import:** `createEndorsementResource` from `'@onderling/agent-registry'`

```js
createEndorsementResource({ pseudoPod, anchorPodUri, deviceId, preferPodUri = false, resourceUri, maxRetries, onPersistentConflict, ensureAccess: ensureAccessHook, now = () => new Date().toISOString(), } = {})
```

createEndorsementResource — append/revoke/list over a shared-readable
endorsement list, with etag-CAS (reused from concurrency.js).

**Parameters**

- `opts` `object`
- `opts.pseudoPod` `object` — injected pod I/O (read/write)
- `[opts.anchorPodUri]` `string`
- `[opts.deviceId]` `string` — strongly recommended; default store path
- `[opts.preferPodUri]` `boolean`
- `[opts.resourceUri]` `string` — explicit override (wins)
- `[opts.maxRetries=3]` `number`
- `[opts.onPersistentConflict]` `(err: Error) => void`
- `[opts.now]` `() => string`
- `[opts.ensureAccess]` `(uri: string) => (any|Promise<any>)` — best-effort real-pod access-control hook. Wired by the app to `@onderling/pod-client`'s `setResourceAccess` (public-read + owner-write for G1; + admin-write for a G3 community catalog). Fires once, best-effort, after the first successful write to a real (https) pod URI; NEVER on a `pseudo-pod://` URI (hermetic no-op). A throwing hook must NOT break the write. Also exposed as `ensureAccess()` for explicit/idempotent use.

**Returns:** `{ append, revoke, list, get, ensureAccess, resourceUri: string }`

## `src/exportRegistry.js`

### `exportProfileRegistry`

**Kind:** function · **Import:** `exportProfileRegistry` from `'@onderling/agent-registry'`

```js
async exportProfileRegistry({ ownerRoot, registry, passphrase, hints = [], argonOpts } = {})
```

Export the profile set to a passphrase-sealed, storage-agnostic artifact (identity step 5A):
the owner root plus a registry snapshot (`registry.list()`), sealed with core `CloudBackup`
(argon2id + xsalsa20poly1305). Returns the sealed envelope bytes; open them again with
`importProfileRegistry`.

**Parameters**

- `a` `object`
- `a.ownerRoot` `object` — a core Bootstrap — re-derives every profile key on import.
- `a.registry` `object` — an @onderling/agent-registry handle (snapshotted via list()).
- `a.passphrase` `string` — the export password ("this file is everything, guard it").
- `[a.hints]` `object[]` — optional recovery hints.
- `[a.argonOpts]` `object` — KDF cost override (tests pass a light one).

**Returns:** `Promise<Uint8Array>` — the sealed envelope — write to a file / purpose-built DB.

### `importProfileRegistry`

**Kind:** function · **Import:** `importProfileRegistry` from `'@onderling/agent-registry'`

```js
async importProfileRegistry({ sealed, passphrase, argonOpts } = {})
```

Open a sealed export → the owner root + the profile registry snapshot.

**Returns:** `Promise<{ ownerRoot: object, registry: object|null }>`

### `restoreProfilesInto`

**Kind:** function · **Import:** `restoreProfilesInto` from `'@onderling/agent-registry'`

```js
async restoreProfilesInto(registry, snapshot)
```

Write an imported snapshot into a (fresh) registry — the pod-less recovery of the profile set.

## `src/governedRequest.js`

### `DEFAULT_GOVERNED_POLICY`

**Kind:** constant · **Import:** `DEFAULT_GOVERNED_POLICY` from `'@onderling/agent-registry'`

Conservative default policy table for governed asks, keyed by context TYPE (`employment`,
`tenancy`, `commerce`). Each rule lists property types/keys that must not be requested
(`forbidTypes`/`forbidKeys`) and keys that only draw a warning (`warnKeys`). An illustrative
starter, not a legal ruling — the table's authoritative source is still an open design decision.

### `checkRequestAllowed`

**Kind:** function · **Import:** `checkRequestAllowed` from `'@onderling/agent-registry'`

```js
checkRequestAllowed(request, contextType, policyTable = DEFAULT_GOVERNED_POLICY, vocabulary = null)
```

Check a Request against a context TYPE. Pure.

**Parameters**

- `request` `object` — a createRequest(...) record
- `contextType` `string` — e.g. 'employment' | 'tenancy' | 'commerce'
- `[policyTable=DEFAULT_GOVERNED_POLICY]` `object`
- `[vocabulary]` `object` — a createVocabulary(...) — resolves an item's type when absent

**Returns:** `{allowed:boolean, forbidden:string[], warn:string[]}`

## `src/loadProfile.js`

### `loadProfile`

**Kind:** function · **Import:** `loadProfile` from `'@onderling/agent-registry'`

```js
async loadProfile({ ownerRoot, profileSeed, profileId, vault, registry } = {})
```

Load (materialize) a profile on this device (identity step 4). The seed comes from the owner
root + `profileId` OR a delegated 32-byte `profileSeed` (gadget case); the profile's
AgentIdentity is persisted into the device `vault`, optionally checked against the registry
entry (throws on a pubKey mismatch), and returned with per-circle address/seed/identity helpers.

**Parameters**

- `a` `object`
- `[a.ownerRoot]` `object` — a core Bootstrap (with a.profileId) — derives the profile seed.
- `[a.profileSeed]` `Uint8Array` — a delegated 32-byte profile seed (gadget case; no owner root).
- `[a.profileId]` `string` — the profile's id (for owner-root derivation + registry check).
- `a.vault` `object` — the device vault the identity persists into.
- `[a.registry]` `object` — optional — verify the materialised key matches the registered profile.

**Returns:** `Promise<{ profileId, identity, pubKey, circleAddress:(c)=>string, circleSeed:(c)=>Uint8Array }>`

## `src/location.js`

### `LOCATION_LADDER`

**Kind:** constant · **Import:** `LOCATION_LADDER` from `'@onderling/agent-registry'`

The location coarseness ladder, COARSEST→finest (propertyVocabulary convention):
`in-area` (a y/n predicate) → `region` → `municipality` → `district` → `coords` (the whole
value). Below `in-area` sits the implicit ∅ — disclosure withholds the key entirely.

### `LOCATION_IN_AREA`

**Kind:** constant · **Import:** `LOCATION_IN_AREA` from `'@onderling/agent-registry'`

The coarsest predicate rung — leaks no raw place, just in-area yes/no (the design's `in-area(y/n)`).

### `isLocationValue`

**Kind:** function · **Import:** `isLocationValue` from `'@onderling/agent-registry'`

```js
isLocationValue(v)
```

True iff `v` is a usable location value (a non-empty label string or a location object).

### `locationLabel`

**Kind:** function · **Import:** `locationLabel` from `'@onderling/agent-registry'`

```js
locationLabel(value)
```

The coarse DISPLAY/label token for a location value (a place NAME), NEVER raw coords.
Prefers the coarsest named field, then a generic label/cell. `null` when nothing usable
(e.g. a value carrying only raw coords — no human place name to show).

**Parameters**

- `value` `string|{label?:string,cell?:string,district?:string,municipality?:string,region?:string}`

**Returns:** `string|null`

### `inArea`

**Kind:** function · **Import:** `inArea` from `'@onderling/agent-registry'`

```js
inArea(value)
```

The in-area PREDICATE: true iff a location is present (the coarsest, place-free rung).

### `locationDescriptor`

**Kind:** function · **Import:** `locationDescriptor` from `'@onderling/agent-registry'`

```js
locationDescriptor(key = 'location')
```

The property-vocabulary descriptor for the location key. Type `coarse-enum` (like
place/availability), ladder `in-area → region → municipality → district → coords`.
Coarsening is FAIL-CLOSED:
  • `coords` (finest) — releases the whole stored value, incl. any raw coordinates.
  • a named coarse rung (district/municipality/region) — a coarse place label, NEVER coords.
  • `in-area` (coarsest) — and any null/unknown rung — the y/n predicate only.
`sensitive` (location + possessions + availability is the burglary vector).

**Parameters**

- `[key='location']` `string`

## `src/makeActorResolver.js`

### `makeActorResolver`

**Kind:** function · **Import:** `makeActorResolver` from `'@onderling/agent-registry'`

```js
makeActorResolver(registry)
```

Adapt an agent-registry handle into a core `ActorResolver`-shaped object whose async
`resolve` / `register` / `revoke` delegate to the registry. Throws INVALID_ARGUMENT when the
registry handle has no `lookup`.

**Parameters**

- `registry` `ReturnType<import('./AgentRegistry.js').createAgentRegistry>`

**Returns:** `ActorResolver`

## `src/offeringsTaxonomy.js`

### `OFFERINGS_TAXONOMY`

**Kind:** constant · **Import:** `OFFERINGS_TAXONOMY` from `'@onderling/agent-registry'`, `'@onderling/agent-registry/offerings-taxonomy'`, `'@onderling/agent-registry/src/offeringsTaxonomy.js'`

The fixed offerings taxonomy: `{ version, categories: [{ id, label:{nl,en}, hint:{nl,en} }] }`.
Frozen; iterate `categories` for UI dropdowns. (identity-resolver re-exports this as
`TAXONOMY` for its existing consumers.)

### `OFFERING_LADDER`

**Kind:** constant · **Import:** `OFFERING_LADDER` from `'@onderling/agent-registry'`, `'@onderling/agent-registry/offerings-taxonomy'`, `'@onderling/agent-registry/src/offeringsTaxonomy.js'`

The offering coarseness ladder, COARSEST→finest (propertyVocabulary convention):
`category` (only the derived `{ categoryId }`) → `full` (the whole `{ text, tags }` item).
Below `category` sits the implicit ∅ — disclosure simply withholds the key.

### `deriveOfferingCategory`

**Kind:** function · **Import:** `deriveOfferingCategory` from `'@onderling/agent-registry'`, `'@onderling/agent-registry/offerings-taxonomy'`, `'@onderling/agent-registry/src/offeringsTaxonomy.js'`

```js
deriveOfferingCategory(item = {}, taxonomy = OFFERINGS_TAXONOMY)
```

Best-effort category for an open offering item — DETERMINISTIC tag/keyword scoring against the
taxonomy, no model. Per category: a tag equal to the category id scores 3; a tag or text word
found in the category's keyword set (id + label + hint words, NL+EN) scores 2 (tags) / 1
(text words); a tag sharing a ≥4-char substring with a keyword scores 1. Highest score wins;
ties resolve to taxonomy order; no evidence at all → `null` (callers keep the offering
uncategorised or ask the user to pick).

**Parameters**

- `item` `{text?:string, tags?:string[]}` — an open offering (kind/extra fields ignored)
- `[taxonomy=OFFERINGS_TAXONOMY]` `{categories?:Array<{id:string,label?:object,hint?:object}>}`

**Returns:** `string|null` — categoryId

### `offeringDescriptor`

**Kind:** function · **Import:** `offeringDescriptor` from `'@onderling/agent-registry'`, `'@onderling/agent-registry/offerings-taxonomy'`, `'@onderling/agent-registry/src/offeringsTaxonomy.js'`

```js
offeringDescriptor(key = 'offering', { taxonomy = OFFERINGS_TAXONOMY } = {})
```

The property-vocabulary descriptor for an offering key. Type `driver` (an offering IS a
driver-like open item), `sensitive`, ladder `category → full`. Coarsening is FAIL-CLOSED: only
the `full` rung releases the item itself; `category` — and any null/unknown rung — collapses to
`{ categoryId }` ONLY (text/tags dropped; a user-picked `item.categoryId` wins over
derivation; no match → `{ categoryId: null }`, which reveals nothing).

**Parameters**

- `[key='offering']` `string`
- `[opts]` `{taxonomy?:object}` — override taxonomy (tests / future per-app overlays)

## `src/profileProperties.js`

### `own`

**Kind:** function · **Import:** `own` from `'@onderling/agent-registry'`

```js
own(value)
```

An OWN property: holds its own value.

### `inherit`

**Kind:** function · **Import:** `inherit` from `'@onderling/agent-registry'`

```js
inherit(from)
```

An INHERIT property: resolves from `from` (a profileId) or, if absent, the default profile.

### `normaliseProperties`

**Kind:** function · **Import:** `normaliseProperties` from `'@onderling/agent-registry'`

```js
normaliseProperties(raw)
```

Strict-allowlist normalise a properties map (used by the registry resource + callers). Frozen.

### `resolveProperty`

**Kind:** function · **Import:** `resolveProperty` from `'@onderling/agent-registry'`

```js
resolveProperty(getProfile, profileId, key, { defaultProfileId = null } = {})
```

Resolve the EFFECTIVE value of `key` for `profileId`, following inherit pointers up the
chain (an entry's `from`, or the default profile; an UNDECLARED key implicitly inherits
from the default) until an OWN value is found. Cycle- and dead-end-safe → `undefined`.

**Parameters**

- `getProfile` `(id:string)=>({properties?:object}|null|undefined)` — lookup a profile by id
- `profileId` `string`
- `key` `string`
- `[opts]` `{ defaultProfileId?: string|null }`

### `effectiveProperties`

**Kind:** function · **Import:** `effectiveProperties` from `'@onderling/agent-registry'`

```js
effectiveProperties(getProfile, profileId, opts = {})
```

Resolve every effective property for a profile — the union of its own declared keys and the
default profile's keys (the ones it implicitly inherits), each resolved through the chain.

### `setOwn`

**Kind:** function · **Import:** `setOwn` from `'@onderling/agent-registry'`

```js
setOwn(properties, key, value)
```

Flip a property to OWN (re-scope, no migration). Returns a NEW frozen properties map.

### `setInherit`

**Kind:** function · **Import:** `setInherit` from `'@onderling/agent-registry'`

```js
setInherit(properties, key, from)
```

Flip a property to INHERIT (`from` optional → the default profile). Returns a NEW frozen map.

## `src/propertyVocabulary.js`

### `PROPERTY_TYPES`

**Kind:** constant · **Import:** `PROPERTY_TYPES` from `'@onderling/agent-registry'`

The valid property-descriptor types: coarse-enum, driver, coded, credential, scalar (frozen).

### `isPropertyType`

**Kind:** function · **Import:** `isPropertyType` from `'@onderling/agent-registry'`

```js
isPropertyType(t)
```

True iff `t` is one of the PROPERTY_TYPES.

### `descriptor`

**Kind:** function · **Import:** `descriptor` from `'@onderling/agent-registry'`

```js
descriptor({ key, type = 'scalar', ladder = null, coarsen = null, sensitivity = 'normal' } = {})
```

Build a validated, frozen descriptor for one property key.

**Parameters**

- `d` `object`
- `d.key` `string`
- `[d.type='scalar']` `string` — one of PROPERTY_TYPES
- `[d.ladder=null]` `string[]` — rung names, COARSEST→finest (null = no ladder)
- `[d.coarsen=null]` `(value:any, rung:?string)=>any` — coarsen a value to a rung (default: identity)
- `[d.sensitivity='normal']` `string` — 'normal' | 'sensitive' | 'special-category'

### `createVocabulary`

**Kind:** function · **Import:** `createVocabulary` from `'@onderling/agent-registry'`

```js
createVocabulary(descriptors = [])
```

A vocabulary over a set of descriptors. Read-only lookups + a value-coarsening helper.

## `src/registerAgentBundle.js`

### `registerAgentBundle`

**Kind:** function · **Import:** `registerAgentBundle` from `'@onderling/agent-registry'`

```js
async registerAgentBundle({ pseudoPod, podDeviceId, agent, opts = {}, onError = null, } = {})
```

Bring up an agent registry against a bundle's pseudo-pod and register the bundle's agent in it
(the module doc above details each field). Soft-fail: a thrown error returns `null` after the
optional `onError` hook, and a missing pubKey / `pseudoPod` / `podDeviceId` also returns `null`.
Re-registration is idempotent (CAS upsert keyed on `agentId`).

**Parameters**

- `args` `object` — `{ pseudoPod, podDeviceId, agent, opts?, onError? }`

**Returns:** `Promise<object|null>` — the live registry handle, or `null` on failure

## `src/request.js`

### `createRequest`

**Kind:** function · **Import:** `createRequest` from `'@onderling/agent-registry'`

```js
createRequest({ requesterId, purpose, items, vocabulary = null } = {})
```

Build a canonical, validated Request.

**Parameters**

- `a` `object`
- `a.requesterId` `string` — who is asking (webid / circle id / bot id)
- `a.purpose` `string` — plain-language "why we ask" (shown to the user)
- `a.items` `Array<{key:string, why:string, type?:string, minRung?:string}>`
- `[a.vocabulary]` `object` — a createVocabulary(...) — validates keys + types when given

**Returns:** `{requesterId:string, purpose:string, items:Array}`

### `requestHash`

**Kind:** function · **Import:** `requestHash` from `'@onderling/agent-registry'`

```js
requestHash(request)
```

Deterministic content hash of a Request (lowercase hex sha-256). The signature signs this.

### `requestKeys`

**Kind:** function · **Import:** `requestKeys` from `'@onderling/agent-registry'`

```js
requestKeys(request)
```

The keys a Request asks for.

## `src/requestForm.js`

### `requestForm`

**Kind:** function · **Import:** `requestForm` from `'@onderling/agent-registry'`

```js
requestForm(request, vocabulary = null)
```

Project a Request into a neutral form-spec.

**Parameters**

- `request` `{requesterId?:string, purpose?:string, items?:Array}` — a createRequest(...) record
- `[vocabulary]` `object` — a createVocabulary(...) — fills each field's type + coarseness ladder

**Returns:** `{ requesterId:string|null, purpose:string|null, fields: Array<{ key:string, type:string|null, label:string, why:string, required:boolean, ladder?:string[] }> }`

## `src/requestGate.js`

### `egressReceipt`

**Kind:** function · **Import:** `egressReceipt` from `'@onderling/agent-registry'`

```js
egressReceipt({ request, released, contextType = null, policyTable, vocabulary = null } = {})
```

Compute a truthful, zero-disclosure receipt of what a Request would take from this device.

**Parameters**

- `a` `object`
- `a.request` `object` — a createRequest(...) record
- `a.released` `object` — output of releasedValues(...) — {key: coarseValue}
- `[a.contextType]` `string` — a governed context TYPE (e.g. 'employment'); ungoverned when absent
- `[a.policyTable]` `object` — a governed-policy table (defaults inside checkRequestAllowed)
- `[a.vocabulary]` `object` — a createVocabulary(...) — resolves item types for the governed check

**Returns:** `{ shared:string[], withheld:string[], nothingLeft:boolean, governed:{allowed:boolean, forbidden:string[], warn:string[]} }`

### `gateEgress`

**Kind:** function · **Import:** `gateEgress` from `'@onderling/agent-registry'`

```js
gateEgress({ request, released, contextType = null, policyTable, vocabulary = null } = {})
```

The enforceable egress gate: decide whether the outbound payload may leave, build it, and
attach the receipt. A governed-forbidden ask is BLOCKED (allow:false, payload:{}) even if
the user disclosed values. Otherwise the payload is exactly the coarse released set.

**Parameters**

- `a` `object` — same fields as egressReceipt(...)

**Returns:** `{ allow:boolean, payload:object, receipt:object }`

## `src/resource.js`

### `RESOURCE_VERSION`

**Kind:** constant · **Import:** `RESOURCE_VERSION` from `'@onderling/agent-registry'`

Current agent-registry wire-format version (v3 = v2 + per-profile `properties` + `ownerFingerprint`).

### `registryResourceUri`

**Kind:** function · **Import:** `registryResourceUri` from `'@onderling/agent-registry'`

```js
registryResourceUri({ anchorPodUri, deviceId, preferPodUri = false })
```

Default registry-resource path for a given pod / device.

**Pseudo-pod-authoritative by default.** The registry lives on the
*pseudo-pod*: `pod-onboarding` seeds it there during provisioning, and
the underlying pseudo-pod can only write to `pseudo-pod://`
URIs. Pod-side mirroring at `<anchorPodUri>/private/agent-registry`
is later work — wired through cache-mode pseudo-pod once
the consuming app composes the registry's pseudo-pod in cache mode.

Callers that want the pod-side path today can pass `preferPodUri: true`
(forces the https:// path; the pseudo-pod must accept it, i.e. be
cache-mode or wrap a pod-client).

### `emptyResource`

**Kind:** function · **Import:** `emptyResource` from `'@onderling/agent-registry'`

```js
emptyResource()
```

Default empty resource body.

### `normaliseResource`

**Kind:** function · **Import:** `normaliseResource` from `'@onderling/agent-registry'`

```js
normaliseResource(raw)
```

Normalise a resource read from the pseudo-pod. Tolerates absent
fields; freezes the result.

## `src/revealLadder.js`

### `presetForRevealLevel`

**Kind:** function · **Import:** `presetForRevealLevel` from `'@onderling/agent-registry'`

```js
presetForRevealLevel(level)
```

The pinned reveal PRESET for an old level name (`ephemeral→handle`, `persona→profile`, `identity→full`), or undefined.

### `revealLevelForPreset`

**Kind:** function · **Import:** `revealLevelForPreset` from `'@onderling/agent-registry'`

```js
revealLevelForPreset(preset)
```

The old level name for a pinned reveal preset (inverse of `presetForRevealLevel`), or undefined.

### `REVEAL_LEVELS`

**Kind:** constant · **Import:** `REVEAL_LEVELS` from `'@onderling/agent-registry'`

Identity levels, LEAST → MOST revealing. `identity` = hand off to the existing pairwise-reveal /
contact machinery. Derived from `REVEAL_PRESETS` order so the two vocabularies can never drift.

### `isRevealLevel`

**Kind:** function · **Import:** `isRevealLevel` from `'@onderling/agent-registry'`

```js
isRevealLevel(l)
```

True iff `l` is one of the REVEAL_LEVELS (`ephemeral` | `persona` | `identity`).

### `revealRank`

**Kind:** function · **Import:** `revealRank` from `'@onderling/agent-registry'`

```js
revealRank(level)
```

Rank a level (0=ephemeral … 2=identity), or -1 for an unknown level. Delegates to the preset rank.

### `nextRevealLevel`

**Kind:** function · **Import:** `nextRevealLevel` from `'@onderling/agent-registry'`

```js
nextRevealLevel(level)
```

The next level up, or the same level when already at the top (`identity`). Delegates to `nextRevealPreset`.

### `ephemeralHandle`

**Kind:** function · **Import:** `ephemeralHandle` from `'@onderling/agent-registry'`

```js
ephemeralHandle(talkId, side = '')
```

A stable, friendly EPHEMERAL handle for a participant in a talk — deterministic from the talk id
(+ an optional side salt so the two participants differ). Same talk → same handle; different talk →
different handle (unlinkable). No PII, no randomness.

**Parameters**

- `talkId` `string`
- `[side='']` `string` — e.g. 'a' | 'b' — distinguishes the two participants of one talk

### `createParticipant`

**Kind:** function · **Import:** `createParticipant` from `'@onderling/agent-registry'`

```js
createParticipant({ talkId, side = 'a', startLevel = 'ephemeral', persona = null } = {})
```

Create a participant's own reveal state for a talk. `selfLevel` starts at `ephemeral` unless the
user's preference (or an explicit start level) opens higher.

**Parameters**

- `a` `object`
- `a.talkId` `string`
- `[a.side='a']` `string`
- `[a.startLevel='ephemeral']` `string` — the per-user default ('persona' = "always show my persona")
- `[a.persona]` `{id:string, name?:string}` — this participant's circle-persona (shown at level ≥ persona)

**Returns:** `{talkId:string, side:string, level:string, persona:object|null}`

### `revealSelf`

**Kind:** function · **Import:** `revealSelf` from `'@onderling/agent-registry'`

```js
revealSelf(self, toLevel)
```

Reveal MORE of yourself (unilateral). Bumps your own level UP to `toLevel`; a request to go to a
lower or equal level is a no-op (you can never un-reveal, and never reveal the other party). Returns
a NEW participant state (never mutates).

**Parameters**

- `self` `{talkId:string, side:string, level:string, persona:object|null}`
- `toLevel` `string`

### `revealNext`

**Kind:** function · **Import:** `revealNext` from `'@onderling/agent-registry'`

```js
revealNext(self)
```

Quick-switch up one rung (the "reveal my persona" tap during an ephemeral talk).

### `presentSelf`

**Kind:** function · **Import:** `presentSelf` from `'@onderling/agent-registry'`

```js
presentSelf(self)
```

What the OTHER side learns about THIS participant at their current level — the projection sent over
the wire. `ephemeral` → only the deterministic handle; `persona` → the circle-persona; `identity` →
a marker that this side has opened to full identity (the contact exchange happens via the existing
pairwise machinery, not here). Never leaks a level the participant hasn't chosen.

**Parameters**

- `self` `{talkId:string, side:string, level:string, persona:object|null}`

## `src/subscriptions.js`

### `createCommunitySubscriptions`

**Kind:** function · **Import:** `createCommunitySubscriptions` from `'@onderling/agent-registry'`

```js
createCommunitySubscriptions({ resolveCommunity, resolveEndorsements: fallbackResolver, initial } = {})
```

createCommunitySubscriptions — manage a user's subscribed communities and
project them to trust-graph roots + a per-endorser resolver.

**Parameters**

- `opts` `object`
- `opts.resolveCommunity` `(circleId: string) => ({ admins?: string[]|(() => (string[]|Promise<string[]>)), list?: () => (object[]|Promise<object[]>) } | Promise<...>)` — for a subscribed circleId, the community's ADMIN pubKeys (the curator roots) and its catalog `list()` (the community's endorsements). Typically `{ admins: circleAdminPubKeys, list: communityCatalog.list }`.
- `[opts.resolveEndorsements]` `(pubKey: string) => (object[]|Promise<object[]>)` — optional fallback per-endorser resolver for endorsers whose records live outside the community catalogs (personal curator pods → transitive WoT).
- `[opts.initial]` `Iterable<string>` — initially-subscribed circleIds.

**Returns:** `{ subscribe, unsubscribe, has, list, roots, resolveEndorsements }`

## `src/trustGraph.js`

### `CURATOR_ROLES`

**Kind:** constant · **Import:** `CURATOR_ROLES` from `'@onderling/agent-registry'`

Card roles that mark a TRUST NODE (walked through) rather than an installable agent.

### `DEFAULT_MAX_DEPTH`

**Kind:** constant · **Import:** `DEFAULT_MAX_DEPTH` from `'@onderling/agent-registry'`

Default maximum candidate depth for `walkTrustGraph`'s bounded BFS over the endorsement graph.

### `walkTrustGraph`

**Kind:** function · **Import:** `walkTrustGraph` from `'@onderling/agent-registry'`

```js
async walkTrustGraph({ roots, endorsementsOf, resolveCard, maxDepth = DEFAULT_MAX_DEPTH, now } = {})
```

walkTrustGraph — bounded BFS over the endorsement graph from `roots`.

**Parameters**

- `opts` `object`
- `opts.roots` `string[]` — curator root pubKeys (depth 0).
- `opts.endorsementsOf` `(endorser: string) => (object[]|Promise<object[]>)` — that endorser's PUBLISHED endorsement records (their pod resource). Only records whose signed `endorser` === the queried pubKey are honoured.
- `opts.resolveCard` `(subject: string, endorsement: object) => (object|null|Promise<object|null>)` — resolves the endorsed subject's Agent Card (then cardHash-verified).
- `[opts.maxDepth=DEFAULT_MAX_DEPTH]` `number` — max candidate depth.
- `[opts.now]` `() => number` — injectable clock (expiry).

**Returns:** `Promise<Array<{ subject, card, depth, endorsers: string[], tags: string[] }>>` — ranked candidates (proximity, then breadth, then id).

## `src/wellKnownCardResolver.js`

### `createWellKnownCardResolver`

**Kind:** function · **Import:** `createWellKnownCardResolver` from `'@onderling/agent-registry'`

```js
createWellKnownCardResolver({ fetch: fetchImpl, baseFor } = {})
```

Build a `resolveCard(subject, endorsement)` function over an injected `fetch` that GETs an
endorsed agent's A2A Agent Card from `<base>/.well-known/agent` (then `agent.json`). The base
URL is the endorsement's `url`/`agentUri` hint, else `baseFor(subject)`; fetch failures or
non-JSON bodies resolve to `null`. The card is returned as-is — cardHash verification stays with
the caller.

**Parameters**

- `opts` `object`
- `opts.fetch` `typeof fetch` — injected fetch (required; keeps tests hermetic)
- `[opts.baseFor]` `(subject: string) => (string|null)` — pubKey → base URL fallback

**Returns:** `(subject: string, endorsement?: object) => Promise<object|null>`

# `@onderling/app-manifest` — API reference

> Generated by `node scripts/api-appendix.mjs` from the exported surface and source JSDoc.
> Do not edit by hand — edit the JSDoc in the source and regenerate. `npm run api-fitness` fails on drift.

Per-app declarative manifest + pure projectors (validateManifest, paramsToJsonSchema, renderChat, renderSlash). The bundle-declaration format feeding the §0 destination substrates (@onderling/interface-registry, @onderling/protocol) and shipping the chat/slash surface those substrates don't cover. Phase: SP-0 (greenfield).

README: [`packages/app-manifest/README.md`](../../packages/app-manifest/README.md) · Index: [docs/api/README.md](README.md)

## `src/atoms.js`

### `ATOMS`

**Kind:** constant · **Import:** `ATOMS` from `'@onderling/app-manifest'`

@type {ReadonlyArray<Atom>}

### `ATOM_VERBS`

**Kind:** constant · **Import:** `ATOM_VERBS` from `'@onderling/app-manifest'`

Canonical atom verbs only (no aliases).

### `ATOM_VERBS_WITH_ALIASES`

**Kind:** constant · **Import:** `ATOM_VERBS_WITH_ALIASES` from `'@onderling/app-manifest'`

Every accepted spelling — canonical verbs AND aliases.

### `isAtom`

**Kind:** function · **Import:** `isAtom` from `'@onderling/app-manifest'`

```js
isAtom(verb)
```

True iff `verb` is a known SDK atom — a canonical verb OR an accepted alias.
(Contrast `isCanonicalVerb` in validate.js, which is the legacy item-store mirror.)

**Parameters**

- `verb` `string`

### `canonicalAtom`

**Kind:** function · **Import:** `canonicalAtom` from `'@onderling/app-manifest'`

```js
canonicalAtom(verb)
```

The canonical atom verb for any accepted spelling, or `null` if it isn't an atom.
e.g. `canonicalAtom('create') → 'add'`, `canonicalAtom('grab') → 'claim'`.

**Parameters**

- `verb` `string`

**Returns:** `string|null`

### `classifyVerb`

**Kind:** function · **Import:** `classifyVerb` from `'@onderling/app-manifest'`

```js
classifyVerb(verb)
```

Classify a verb into its atom, or `null` for a domain/unknown verb.

**Parameters**

- `verb` `string`

**Returns:** `(Atom & { canonical: string, viaAlias: boolean }) | null`

### `atomFor`

**Kind:** function · **Import:** `atomFor` from `'@onderling/app-manifest'`

```js
atomFor(canonicalVerb)
```

The Atom for a canonical verb (no alias resolution), or `undefined`.

## `src/capabilities.js`

### `opNouns`

**Kind:** function · **Import:** `opNouns` from `'@onderling/app-manifest'`

```js
opNouns(op, itemTypes = [])
```

The noun types an op names — via appliesTo.type (string|array|'*') and/or a type-enum param.

### `capabilityKey`

**Kind:** function · **Import:** `capabilityKey` from `'@onderling/app-manifest'`

```js
capabilityKey(app, atom, noun)
```

Stable key for one (app × atom × noun) capability. The single spelling both the gate and the
freedom template use (the space separators can't occur in app/atom/noun tokens). Lives here so
capabilityGate (canopy-chat) and freedom.js agree by construction.

### `resolveAtom`

**Kind:** function · **Import:** `resolveAtom` from `'@onderling/app-manifest'`

```js
resolveAtom(manifest, atom, noun)
```

The opId implementing the capability (atom × noun), or `null` if none does.
`atom` may be an alias — it's canonicalised first.

**Returns:** `string|null`

### `resolveCapability`

**Kind:** function · **Import:** `resolveCapability` from `'@onderling/app-manifest'`

```js
resolveCapability(manifest, atom, noun)
```

Resolve HOW a capability `(atom × noun)` is served (PLAN §1b):
 - `{ kind:'op', opId }`            — a bespoke op implements it → dispatch that op.
 - `{ kind:'generic', atom, noun }` — the noun DECLARES the atom but no op implements it → serve with the
                                      generic store-backed handler (`createGenericAtomHandlers`). This is
                                      "declare a noun → get CRUD free": a new app declares the atom on the
                                      noun and the CRUD is served unwritten.
 - `{ kind:'none' }`                — not a capability of this manifest (non-atom, or undeclared + unimplemented).
`atom` may be an alias — it's canonicalised first.

**Returns:** `{kind:'op', opId:string} | {kind:'generic', atom:string, noun:string} | {kind:'none'}`

### `capabilitiesOf`

**Kind:** function · **Import:** `capabilitiesOf` from `'@onderling/app-manifest'`

```js
capabilitiesOf(manifest)
```

The full deduped (verb × noun) capability set of a manifest.

DECLARED-AUTHORITATIVE (decision 2026-07-02, docs/decisions.md): when a manifest DECLARES `nouns`,
that declaration IS its member-facing capability surface — the set is exactly the declared
(noun × atom) pairs and ops only FILL the implementing `opId`. A pair an op would DERIVE but the
author did NOT declare is dropped (the author curates; e.g. a broad `appliesTo` no longer mints
capabilities on internal/view-shape itemTypes). Without a `nouns` declaration, ops ARE the surface
(every op's atom × noun is derived) — the fallback for un-migrated manifests, so the gate works
app-wide before every app declares nouns.

**Returns:** `Capability[]`

### `atomsForNoun`

**Kind:** function · **Import:** `atomsForNoun` from `'@onderling/app-manifest'`

```js
atomsForNoun(manifest, noun)
```

The canonical atom verbs available for a given noun (declared ∪ derived), sorted.

## `src/conformance.js`

### `manifestConformance`

**Kind:** function · **Import:** `manifestConformance` from `'@onderling/app-manifest'`

```js
manifestConformance(manifest)
```

Assert a single app manifest against the conformance standard.

**Parameters**

- `manifest` `import('./schema.js').Manifest`

**Returns:** `{ ok: boolean, issues: ConformanceIssue[], warnings: Array<{path?: string, message: string, code?: string}> }` — `ok` reflects `issues` only. `warnings` carries non-blocking signals (registry-noncanonical nouns via F-SP1-a) — present but never flipping `ok`.

## `src/dispatchAtom.js`

### `dispatchAtom`

**Kind:** function · **Import:** `dispatchAtom` from `'@onderling/app-manifest'`

```js
async dispatchAtom(manifest, { atom, noun, args = {} } = {}, dispatch)
```

Invoke a manifest capability by its ATOM + NOUN instead of its bespoke op-id. `resolveAtom` maps
the (atom × noun) pair to an op-id against the manifest's capability surface (alias atoms are
canonicalised first); the caller-supplied `dispatch(opId, args)` then runs the existing handler.
Pure and transport/reply-shape-agnostic — it routes to per-op handlers, it does not replace them.

**Parameters**

- `manifest` `object` — the app manifest whose capability surface is consulted.
- `cap` `{atom:string, noun:string, args?:object}` — the capability call.
- `dispatch` `(opId:string, args:object) => any|Promise<any>` — runs the resolved op.

**Returns:** `Promise<{ok:true, opId:string, result:any} | {ok:false, code:'unimplemented'|'no-dispatch', atom?:string, noun?:string, opId?:string}>` — `unimplemented` = no op implements the (atom × noun); `no-dispatch` = `dispatch` was missing.

### `dispatchCapability`

**Kind:** function · **Import:** `dispatchCapability` from `'@onderling/app-manifest'`

```js
async dispatchCapability(manifest, { atom, noun, args = {} } = {}, { dispatch, generic = {}, ctx = {} } = {})
```

dispatchCapability — the full §1b router: resolve a capability `(atom × noun)` and run it via the bespoke
op if one exists (`dispatch(opId, args)`), OR the generic store-backed handler if the noun merely DECLARES
the atom (`generic[atom](noun, args, ctx)`). So a caller invokes by (atom, noun) and gets either the app's
bespoke behaviour or free CRUD — "declare a noun, get CRUD". `generic` is the map from
`createGenericAtomHandlers(store)` (item-store); passed in so app-manifest keeps no store dependency.

**Parameters**

- `manifest` `object`
- `cap` `{atom:string, noun:string, args?:object}`
- `deps` `{ dispatch:(opId:string,args:object)=>any, generic?:Record<string,Function>, ctx?:object }`

**Returns:** `Promise<{ok:true, via:'op'|'generic', opId?:string, atom?:string, result:any} | {ok:false, code:'unimplemented'|'no-dispatch'|'no-generic', opId?:string, atom?:string, noun?:string}>`

## `src/freedom.js`

### `FREEDOM_LEVELS`

**Kind:** constant · **Import:** `FREEDOM_LEVELS` from `'@onderling/app-manifest'`

Whether a member MUST use a capability or MAY opt out (ruling Q3).

### `OPT_OUT_CONSEQUENCES`

**Kind:** constant · **Import:** `OPT_OUT_CONSEQUENCES` from `'@onderling/app-manifest'`

What happens in the UI when a member opts out of an optional capability (ruling Q3).

### `DEFAULT_ROW`

**Kind:** constant · **Import:** `DEFAULT_ROW` from `'@onderling/app-manifest'`

The freedom row for a capability the admin template says nothing about (default-on, Q5).

### `buildCapabilityMatrix`

**Kind:** function · **Import:** `buildCapabilityMatrix` from `'@onderling/app-manifest'`

```js
buildCapabilityMatrix(sources, { enabledApps = null, template = {}, optOuts } = {})
```

The full per-capability matrix the wizard renders: one row per (app × atom × noun) capability of the
ENABLED apps, merged with the admin `template`. Rows carry the resolved freedom/consequence + the
implementing `opId` (or null = declared-but-unimplemented), plus (B · Slice 4) whether a member may
opt out (`optOutable` = freedom 'optional' OR a privacy floor) and whether THIS member has (`optedOut`).

**Parameters**

- `sources` `Array<{manifest:object}>`
- `[opts]` `object`
- `[opts.enabledApps]` `Set<string>|string[]|null` — apps enabled in the circle (null = all)
- `[opts.template]` `object` — `{ "<app> <atom> <noun>": { enabled?, freedom?, consequence?, privacyFloor? } }`
- `[opts.optOuts]` `Set<string>|string[]` — the member's opted-out capability keys (Slice 4)

**Returns:** `Array<{key,app,atom,noun,opId,enabled,freedom,consequence,privacyFloor,optOutable,optedOut}>`

### `effectiveCapabilityKeys`

**Kind:** function · **Import:** `effectiveCapabilityKeys` from `'@onderling/app-manifest'`

```js
effectiveCapabilityKeys(sources, opts = {})
```

The narrowed capability KEY SET the gate authorises = admin-template ∩ (not member opt-outs).
A cap is authorised iff its matrix row is `enabled` (admin didn't disable it) AND the member hasn't
opted out of it (only opt-outable caps can be opted out). No `optOuts` ⇒ pure admin template (Slices 1–2).

**Returns:** `Set<string>`

### `affordanceTreatment`

**Kind:** function · **Import:** `affordanceTreatment` from `'@onderling/app-manifest'`

```js
affordanceTreatment(matrix, { app, atom, noun } = {})
```

B · Slice 4 (ruling Q3) — how an AFFORDANCE for a capability should render for this member, given a
pre-built matrix (`buildCapabilityMatrix` with the member's `optOuts`). An authorised cap renders
normally (`'show'`); a disabled-or-opted-out cap applies the admin's consequence:
  greyed → `'grey'` (show but disabled) · hidden → `'hide'` (omit) · limited → `'limit'`.
A cap not in the matrix (domain verb / not gated) → `'show'`. Pure; surfaces map the result to UI.

**Parameters**

- `matrix` `Array` — from buildCapabilityMatrix({..., optOuts})
- `cap` `{app:string, atom:string, noun:string}`

**Returns:** `'show'|'grey'|'hide'|'limit'`

## `src/genericOp.js`

### `encodeGenericOpId`

**Kind:** function · **Import:** `encodeGenericOpId` from `'@onderling/app-manifest'`

```js
encodeGenericOpId(app, atom, noun)
```

Encode a generic capability into its synthetic op-id.

### `isGenericOpId`

**Kind:** function · **Import:** `isGenericOpId` from `'@onderling/app-manifest'`

```js
isGenericOpId(opId)
```

True iff `opId` is a synthetic generic op-id (cheap prefix check for the dispatch hot path).

### `decodeGenericOpId`

**Kind:** function · **Import:** `decodeGenericOpId` from `'@onderling/app-manifest'`

```js
decodeGenericOpId(opId)
```

Decode a synthetic op-id back to `{ app, atom, noun }`, or `null` when it isn't one / is malformed.

**Returns:** `{app:string, atom:string, noun:string}|null`

## `src/paramsToJsonSchema.js`

### `paramsToJsonSchema`

**Kind:** function · **Import:** `paramsToJsonSchema` from `'@onderling/app-manifest'`

```js
paramsToJsonSchema(params, opts = {})
```

params[] → plain JSON Schema object.  Output shape matches what the
current household `V0_TOOL_CATALOG` feeds the LLM — no `$schema`, no
draft pragma — so SP-1's byte-equivalence gate can diff cleanly.

Properties + required preserve param declaration order (determinism;
cf. internal/order.js).

**Parameters**

- `params` `Array<import('./schema.js').Param>`
- `[opts]` `{ manifest?: import('./schema.js').Manifest }` — `manifest` is consulted only when a param has `kind:'enum' of:'itemTypes'` to resolve the enum values against `manifest.itemTypes`.

**Returns:** `{ type: 'object', properties: object, required: string[] }`

## `src/renderChat.js`

### `renderChat`

**Kind:** function · **Import:** `renderChat` from `'@onderling/app-manifest'`

```js
renderChat(manifest, args, opts = {})
```

Render the chat-surface projection of a manifest: `toolCatalog` + `toolHandlers` + `systemPrompt`
(the `ChatAgent` ctor shape) plus the structured chat affordance lookups (`commandMenu`,
`inlineKeyboardFor`, `replyShapeFor`, `followUpsFor`, `runtimeFor`, `embedSnapshotFor`, `briefFor`,
`searchFor`). Ops without a matching skill in `skillRegistry` are omitted from `toolHandlers`;
outputs follow manifest declaration order. Throws when `manifest`, `skillRegistry`, or
`toSkillCtx` is missing.

**Parameters**

- `manifest` `import('./schema.js').Manifest`
- `args` `object`
- `args.skillRegistry` `Record<string, function>` — `{ opId: skill }` app-side skills.
- `args.toSkillCtx` `(toolCtx: object) => object` — maps a ChatAgent tool ctx to a skill ctx.
- `[args.onStateUpdates]` `(stateUpdates: Array<object>) => void` — sink for a skill's stateUpdates (an error thrown here is logged, never allowed to kill the reply).
- `[opts]` `object`
- `[opts.prompt]` `{preamble?: string, perToolLine?: function, postamble?: string}` — prompt builder overrides (ignored when the manifest carries a verbatim `systemPrompt` string).

**Returns:** `{toolCatalog: Array<object>, toolHandlers: Record<string, function>, systemPrompt: string, commandMenu: Array<object>, inlineKeyboardFor: function, replyShapeFor: function, followUpsFor: function, runtimeFor: function, embedSnapshotFor: function, briefFor: function, searchFor: function}`

## `src/renderCoverage.js`

### `renderCoverage`

**Kind:** function · **Import:** `renderCoverage` from `'@onderling/app-manifest'`

```js
renderCoverage(manifestOrList)
```

Scan a manifest (or several) into the surface-coverage matrix: one row per op with a boolean per
surface (chat / slash / gate / web-mobile screen / inline), read from the same `surfaces.*`
declarations the projectors consume — plus per-surface totals. Pure and declaration-ordered.

**Parameters**

- `manifestOrList` `import('./schema.js').Manifest | import('./schema.js').Manifest[]`

**Returns:** `{ surfaces: Array<{key:string,label:string}>, rows: Array<{app:string, op:string, verb:string, chat:boolean, slash:boolean, gate:boolean, screen:boolean, inline:boolean, gateVerbs:string[]}>, totals: Record<string,number> }`

### `coverageGaps`

**Kind:** function · **Import:** `coverageGaps` from `'@onderling/app-manifest'`

```js
coverageGaps(coverage, surfaceKey)
```

Ops missing a given surface — the work list for that surface (e.g. coverageGaps(cov, 'gate')).

### `formatCoverageMarkdown`

**Kind:** function · **Import:** `formatCoverageMarkdown` from `'@onderling/app-manifest'`

```js
formatCoverageMarkdown(coverage, { mark = '✅', blank = '·' } = {})
```

Render the matrix as a scannable markdown table (grouped by app).

## `src/renderGate.js`

### `renderGate`

**Kind:** function · **Import:** `renderGate` from `'@onderling/app-manifest'`

```js
renderGate(manifestOrList, opts = {})
```

Render a manifest (or several) into deterministic token-gate rules — one `{ name, test, command }`
rule per manifest, wrapping `renderSlash`'s matcher. `command(text)` returns
`{ opId, args, appOrigin } | null` (null → the engine falls through to the next rule / the LLM);
declaration order and first-match-wins are preserved, and a multi-item parse dispatches only the
FIRST command.

**Parameters**

- `manifestOrList` `import('./schema.js').Manifest | import('./schema.js').Manifest[]`
- `[opts]` `object` — forwarded to `renderSlash` (e.g. `{ locale, trailLexicon }`).

**Returns:** `Array<{ name:string, test:()=>boolean, command:(text:string)=>({opId:string,args:object,appOrigin?:string}|null) }>`

## `src/renderSlash.js`

### `renderSlash`

**Kind:** function · **Import:** `renderSlash` from `'@onderling/app-manifest'`

```js
renderSlash(manifest, opts = {})
```

Render the deterministic, generic slash-grammar matcher.

`parse(text)` returns the same shape household's `regexParse` does:
    null                                  (no match — caller falls back)
  | { skillId, args }
  | Array<{ skillId, args }>              (multi-item, when an op opts
                                           into splitItems)

Drop-in for the SP-1 byte-equivalence gate (PLAN §1.4).

Driven by:
  - manifest.slashGrammar              (addressedPrefixes, specials,
                                         typeAliases, defaultType)
  - per-op surfaces.slash.match        (verbs, body, splitItems, onEmpty)

Pure.  Deterministic.  Verbs / operations tried in declaration order;
first match wins.  Patterns compiled once per `renderSlash` call so
`parse` stays cheap.

Per PLAN flag #13 (F-SP1-b, locked 2026-05-19): the grammar spec is
rich enough to encode household's regexParse — EN/NL aliases, multiword
verb phrases ('voeg toe'), special forms ('what do we need'), item
splitting on `,`/` and `/` en ` with quote handling, peel-type +
default-type fallback, trailing-punct strip, addressed-prefix strip.

**Parameters**

- `manifest` `import('./schema.js').Manifest`

## `src/renderWeb.js`

### `renderMobile`

**Kind:** function · **Import:** `renderMobile` from `'@onderling/app-manifest'`

Defined as `renderWeb` in the source module.

```js
renderWeb(manifest)
```

Render the web/mobile-surface projection of a manifest: a platform-neutral `NavModel`
(`{ app, sections, globals }`, plus `pages` / `tabs` / `actions` keys only when the manifest
declares them). Deterministic — sections follow `manifest.views` order, affordances follow
`manifest.operations` order. `renderMobile` re-exports this function, so renderWeb ≡ renderMobile
holds by construction. Throws when `manifest` is missing.

**Parameters**

- `manifest` `object`

**Returns:** `NavModel`

### `renderWeb`

**Kind:** function · **Import:** `renderWeb` from `'@onderling/app-manifest'`

```js
renderWeb(manifest)
```

Render the web/mobile-surface projection of a manifest: a platform-neutral `NavModel`
(`{ app, sections, globals }`, plus `pages` / `tabs` / `actions` keys only when the manifest
declares them). Deterministic — sections follow `manifest.views` order, affordances follow
`manifest.operations` order. `renderMobile` re-exports this function, so renderWeb ≡ renderMobile
holds by construction. Throws when `manifest` is missing.

**Parameters**

- `manifest` `object`

**Returns:** `NavModel`

## `src/schema.js`

### `__types__`

**Kind:** constant · **Import:** `__types__` from `'@onderling/app-manifest'`

Deliberate typedef-carrier marker — the module's only runtime export, always `true`. `schema.js`
exists for its JSDoc `@typedef`s (doc/IDE artefacts, no runtime validation); this constant makes
the file a real ES module so imports of the schema subpath resolve cleanly.

## `src/settings.js`

### `settingsOf`

**Kind:** function · **Import:** `settingsOf` from `'@onderling/app-manifest'`

```js
settingsOf(manifest, { scope } = {})
```

The declared settings, optionally filtered to a `scope` ('circle' default when a setting omits it).

### `settingDefaults`

**Kind:** function · **Import:** `settingDefaults` from `'@onderling/app-manifest'`

```js
settingDefaults(manifest, opts = {})
```

`{ key: default }` for every setting (in `scope`) that declares a default — seeds a wizard/form.

### `isSettingRequired`

**Kind:** function · **Import:** `isSettingRequired` from `'@onderling/app-manifest'`

```js
isSettingRequired(setting, values = {})
```

Resolve `requiredWhen`: a setting is required iff EVERY named sibling in `values` matches its
allowed value (or one of an allowed array). No `requiredWhen` → never conditionally-required.

**Parameters**

- `setting` `object` — a Setting declaration
- `[values]` `object` — the current form values (sibling keys → chosen values)

**Returns:** `boolean`

### `buildSettingsForm`

**Kind:** function · **Import:** `buildSettingsForm` from `'@onderling/app-manifest'`

```js
buildSettingsForm(manifest, { scope, values = {} } = {})
```

Project `manifest.settings` into render-ready FORM FIELDS (B · Slice 2) — the pure layer the web
+ mobile wizard/settings shells draw from (they own only the widgets). Each field resolves its
current `value` (supplied `values` ?? declared default) and conditional `required` (via
`isSettingRequired`), so the shell renders without re-deriving anything. `control` is the setting
`kind` verbatim (toggle|choice|text|number|member) — the shell maps it to a platform widget.

**Parameters**

- `manifest` `object`
- `[opts]` `object`
- `[opts.scope]` `'circle'|'user'` — restrict to one scope (admin template vs member prefs)
- `[opts.values]` `object` — current values (overrides defaults; drives requiredWhen)

**Returns:** `Array<{key,label,control,choices?,value,required,adminOnly,hint?,scope}>`

## `src/validate.js`

### `isRegistryType`

**Kind:** function · **Import:** `isRegistryType` from `'@onderling/app-manifest'`

```js
isRegistryType(t)
```

L4 ≡ B — registry recognition. True when the shared `@onderling/item-types`
registry recognises `t` as a declared noun/item-type, either by its
canonical name OR a legacy alias (`registryTypeMetadata` resolves aliases,
returning `null` only for genuinely unknown names). This is the single
source-of-truth check the manifest validator uses to converge a manifest's
declared nouns (`manifest.itemTypes`, of which `manifest.nouns` keys are a
subset) with the registry.

**Parameters**

- `t` `string`

**Returns:** `boolean`

### `VERBS`

**Kind:** constant · **Import:** `VERBS` from `'@onderling/app-manifest'`

Frozen verb allow-list mirroring `@onderling/item-store` `ItemStore`
methods.  Operations must declare a `verb` from this set.

### `SETTING_KINDS`

**Kind:** constant · **Import:** `SETTING_KINDS` from `'@onderling/app-manifest'`

B · Slice 2 (ruling Q1) — frozen allow-list of setting `kind`s (the wizard/form renderer
picks a control per kind).  `toggle`=checkbox, `choice`=select(of), `text`/`number`=inputs,
`member`=circle-member picker.  Distinct from PARAM_KINDS (settings are user-facing config,
not op arguments).

### `SETTING_SCOPES`

**Kind:** constant · **Import:** `SETTING_SCOPES` from `'@onderling/app-manifest'`

B · Slice 2 — a setting's resolution scope: `circle` = admin template, `user` = member pref.

### `COMPOSITE_ON_ERROR`

**Kind:** constant · **Import:** `COMPOSITE_ON_ERROR` from `'@onderling/app-manifest'`

P1 (feedback-extension DESIGN §1.3) — frozen allow-list of an
`Operation.onError` policy for composite ops.  'stop' (default when
absent) halts the composite on the first failing step; 'continue'
runs every step best-effort.  No rollback either way (v0 non-goal).

### `NAV_TARGET_KINDS`

**Kind:** constant · **Import:** `NAV_TARGET_KINDS` from `'@onderling/app-manifest'`

Nav-chrome (D / Surface 1) — frozen allow-list of a NavItem target's
`kind`.  The SHARED nav-chrome vocabulary both `manifest.tabs` (Surface 1)
and the future `manifest.actions` / nav-actions (Surface 2) key off:
  'nav' — selects an app-nav root that maps to NO op (`target.to`).
  'op'  — selects/dispatches a manifest op (`target.opId`).

### `isCanonicalVerb`

**Kind:** function · **Import:** `isCanonicalVerb` from `'@onderling/app-manifest'`

```js
isCanonicalVerb(verb)
```

@param {string} verb

### `validateManifest`

**Kind:** function · **Import:** `validateManifest` from `'@onderling/app-manifest'`

```js
validateManifest(manifest, opts = {})
```

Validate a manifest.

**Parameters**

- `manifest` `import('./schema.js').Manifest`
- `[opts]` `object`
- `[opts.strict=false]` `boolean` — V0.4 Q16 (2026-05-21) — when `true`, every `view.dataSource.skillId` AND every `view.fields[].patch.opId` must be either declared in `manifest.operations[].id` OR in the new `manifest.externalSkills` allow-list. Catches typos (e.g. `'getMispelled'`) at manifest level. Default: non-strict (existing tolerant behaviour).
- `[opts.atoms=false]` `boolean` — B · Layer 1 (2026-07-01) — ATOM DISCIPLINE. When `true`, every `op.verb` must be a known SDK atom (or alias — see `atoms.js`) OR be declared in `manifest.domainVerbs`. This is the fitness function against verb drift: a new noun-specific verb can't sneak in without either mapping to an atom or being explicitly named as domain-specific. Default off (F-SP1-e tolerant behaviour preserved for older callers).
- `[opts.strictNouns=false]` `boolean` — L4 ≡ B (2026-07-05) — REGISTRY-NOUN DISCIPLINE. The `@onderling/item-types` registry is the source of truth for nouns. By DEFAULT every declared `manifest.itemTypes` entry (and, transitively, every `manifest.nouns` key — nouns keys must be a subset of itemTypes) that the registry does not recognise is reported as a NON-BLOCKING WARNING (`result.warnings`, code `noncanonical-itemtype`) — F-SP1-a app-local types keep validating. When `true`, those same findings become hard ERRORS (default-deny toward `(verb × noun) = (atom × item-type)`): a manifest may only declare nouns the shared registry knows. Opt-in so existing apps that still carry app-local nouns don't hard-break.

**Returns:** `{ ok: boolean, errors: Array<{path: string, message: string}>, warnings: Array<{path: string, message: string, code?: string}> }` — `ok` reflects `errors` only — warnings never flip `ok`. `warnings` is always present (empty array when clean); forward-additive for callers that only read `{ ok, errors }`.

### `classifyItemTypes`

**Kind:** function · **Import:** `classifyItemTypes` from `'@onderling/app-manifest'`

```js
classifyItemTypes(manifest)
```

Informational helper: split a manifest's `itemTypes` into canonical
(registered in `@onderling/item-types` `list()`) vs app-local.

`validateManifest` does NOT reject app-local types (F-SP1-a); this is
pure introspection for tooling / docs / debug output.

**Parameters**

- `manifest` `import('./schema.js').Manifest`

**Returns:** `{ canonical: string[], appLocal: string[] }`

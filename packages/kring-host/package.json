{
  "name":        "@canopy/kring-host",
  "version":     "0.1.0",
  "description": "The platform-neutral kring/circle HOST substrate — composition LOGIC shared by the web + mobile canopy-chat shells (repo-split W2, objective F). Depends DOWN on the kernel substrates (@canopy/item-store, @canopy/item-types); never up on an app. First extraction: the circle LISTS feature (makeCircleLists).",
  "type":        "module",
  "main":        "src/index.js",
  "exports": {
    ".":             "./src/index.js",
    "./circleLists": "./src/circleLists.js"
  },
  "scripts": {
    "test":       "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@canopy/item-store": "workspace:*",
    "@canopy/item-types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}

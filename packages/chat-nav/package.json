{
  "name":        "@canopy/chat-nav",
  "version":     "0.1.0",
  "description": "Tiny substrate for the canopy-chat ⇄ side-panel navigation protocol (B.1).  No framework — vanilla JS + DOM.  RN consumers compose the same helpers via the matching @canopy/chat-nav/rn export when that ships.",
  "type":        "module",
  "main":        "index.js",
  "exports": {
    ".":          "./index.js",
    "./returnTo": "./src/returnTo.js",
    "./button":   "./src/floatingButton.js"
  },
  "scripts": {
    "test":       "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest":    "^3.0.0",
    "happy-dom": "^15.0.0"
  }
}

{
  "private": true,
  "name": "@canopy/pod-search",
  "version": "0.1.0",
  "description": "FTS5 wrapper + faceted filter over pod content (Archive, Notes search, Task search).",
  "type": "module",
  "main": "src/index.js",
  "exports": { ".": "./src/index.js" },
  "scripts": { "test": "vitest run", "test:watch": "vitest" },
  "devDependencies": {
    "@canopy/pseudo-pod": "file:../pseudo-pod",
    "vitest": "^2.1.0"
  }
}

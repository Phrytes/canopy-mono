{
  "private": true,
  "name": "@onderling/offering-match",
  "version": "0.2.0",
  "description": "Pubsub-of-offerings + posture flag + closed-group governance for matchmaking + claim flows. Composes core.Agent + pubSub.js — no synthetic transport. OfferingMatch is the exported class.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": { "test": "vitest run", "test:watch": "vitest" },
  "dependencies": {
    "@onderling/core": "file:../core",
    "@onderling/vault": "file:../vault"
  },
  "devDependencies": { "vitest": "^2.1.0" }
}

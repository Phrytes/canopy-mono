// iOS native bindings stub.
//
// Placeholder for future BLE (CoreBluetooth) and mDNS (NetService /
// NWBrowser) implementations.  Keep this file as a non-empty target so
// CocoaPods is happy and downstream apps can build.  JS-side BleTransport
// and MdnsTransport throw a clear "not available on this platform" error
// when invoked without a native companion.
import Foundation

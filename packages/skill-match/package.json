{
  "name": "@canopy/skill-match",
  "version": "0.2.0",
  "description": "Pubsub-of-skills + posture flag + closed-group governance for matchmaking + claim flows. Composes core.Agent + pubSub.js — no synthetic transport.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": { "test": "vitest run", "test:watch": "vitest" },
  "dependencies": {
    "@canopy/core": "file:../core"
  },
  "devDependencies": { "vitest": "^2.1.0" }
}

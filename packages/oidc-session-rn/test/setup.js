/**
 * Test setup is now a no-op — index.js no longer imports any Expo
 * modules at module-load (the hook lives at the /hook subpath).
 *
 * Kept as a placeholder so future tests can hang setup-time mocks
 * here without changing vitest.config.js.
 */

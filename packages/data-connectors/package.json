{
  "private": true,
  "name": "@onderling/data-connectors",
  "version": "0.1.0",
  "description": "External data-source connector substrate: connect an agent to DBs / REST APIs / other sources (+ pluggable auth) as source-agnostic, injected adapters — same genre as llm-client / blob-gateway. v0: REST + SQL connectors, injected fetch/driver, mock-tested, no live service in CI.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".":                  "./src/index.js",
    "./types":            "./src/types.js",
    "./errors":           "./src/errors.js",
    "./auth":             "./src/auth.js",
    "./connectors/rest":  "./src/connectors/rest.js",
    "./connectors/sql":   "./src/connectors/sql.js",
    "./capability":       "./src/capability.js"
  },
  "scripts": {
    "test":       "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}

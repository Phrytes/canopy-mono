// Lifted to @canopy/sync-engine.  Folio re-exports for backwards
// compatibility with internal callers + tests that still import from here.
export { fsNode, createFsNode } from '@canopy/sync-engine/adapters/fsNode';

# Public entrypoints. Caddy fetches + renews TLS automatically. {$VAR} are read from the
# caddy service's environment (set in docker-compose.yml from deploy/.env).

{$PODS_HOST} {
	reverse_proxy css:3000
}

{$ACTIVATE_HOST} {
	reverse_proxy activation:8787
}

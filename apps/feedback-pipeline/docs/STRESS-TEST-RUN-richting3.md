# Stress-test run output — Richting 3 (whistleblowing)

Verbatim output of the hardened pipeline on the adversarial dataset
(`/tmp/stress-r3.json`: 18 messages, 10 users, generated by 6 whistleblower
agents + 1 red-team agent). k=3, language Dutch. This is the run that closed the
silent-loss gap (audit 3). See `STRESS-TEST-RESULTS.md` for the verdicts and
`STRESS-TEST-AGENTS.md` for the harness.

**10 users · 18 messages · k=3** — every message lands in one of the four
tracks below; `dropped` is empty (nothing silently deleted).

## 📊 Statistical track (≥3 distinct users, k-anonymous)

**fraud — 4 users\* / 6 messages**
> Er wordt vermoed dat facturen inkoop worden vervalsd; mogelijke bedragen van
> tienduizenden tot vijftigduizenden euro. Anonieme melding mogelijk via e-mail
> of telefoonnummer.

\* Audit note: only w1/w2/w3 are genuine fraud reporters; **w10 (a PII-only
"contact me" message) was LLM-labelled "fraud" and inflated the count** — fixed
by refinement A (PII-only messages routed to their own track).

## 🚨 Signal track (escalated, no threshold)

- **safety / high** (w5, via safety-lexicon) — scaffolding/VCA fraud, "het is
  wachten op een dode hier" (×2 messages).
- **safety / high** (w6, via safety-lexicon) — QC samples logged as passed,
  "someone could get hurt".
- **integrity / high** (w8, via LLM) — corruption report (×3 messages; this is
  the sybil — same user, visible repetition).
- **crisis / high** (w11, via crisis-lexicon) — buried self-harm line
  "Soms wil ik er gewoon niet meer zijn".

## 🔎 Review queue (sensitive, below threshold — quarantined, NOT deleted)

- **sexual harassment** (w4, via domain) — Marco Brouwer demanding favours; "ik
  ben niet de enige", bystander names redacted to `[naam]`.
- **harassment** (w3, via domain) — procurement fear-to-speak (LLM mislabelled;
  safely quarantined).
- **safety** (w6, via domain) — the fearful "please don't share my name" intro.
- **workload** (w9, **via content/re-id**) — the discrimination report that the
  LLM mislabelled "workload"; saved by the deterministic re-identification
  detector on the raw text. *This is the audit-2 → audit-3 fix in action.*

## 🗑️ Dropped (non-sensitive, below threshold)

_empty_ — nothing was silently deleted.

---

## How to read this

- **No serious report was lost.** The previously-deleted self-harm (w11),
  fatal-safety (w5), harassment (w4) and discrimination (w9) reports are all in
  signals or review.
- **Quarantine is deterministic** — w9 reached review via `content/re-id`
  *despite* the wrong LLM theme label.
- **Two residuals visible here** (addressed by refinements A & B): the inflated
  fraud count (w10 swept in) and the soft "workload" label on w9's review item.
- Names of *accused* individuals are kept (policy: accountability is a feature);
  *bystander* names (Pawel, Sanne, Peter) are redacted to `[naam]`/"iemand";
  these tracks go to a human responder, not the public aggregate.

Regenerate with: `node scripts/run-dataset.js /tmp/stress-r3.json 3`.

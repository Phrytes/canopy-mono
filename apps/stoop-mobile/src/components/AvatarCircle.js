/**
 * AvatarCircle — re-export of the lifted substrate component.
 *
 * Lifted to `@canopy/react-native/components` 2026-05-09 (Phase
 * 41.0.b B1). Stoop's tree is wrapped in `<ThemeProvider value={...}>`
 * (in `App.js`) which feeds the substrate component its tokens via
 * `useTheme()`.
 */
export { AvatarCircle } from '@canopy/react-native/components';

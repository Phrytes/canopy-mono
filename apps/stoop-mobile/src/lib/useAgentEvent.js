/**
 * useAgentEvent — re-exported from `./useSkill.js` after the Phase 41.0
 * L1 lift (2026-05-09). Both hooks are produced by the same
 * `createReactBindings` factory call so they share the bound
 * ServiceContext.
 */
export { useAgentEvent } from './useSkill.js';

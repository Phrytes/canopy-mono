/**
 * useSkillResult — re-exported from `./useSkill.js` after the Phase 41.0
 * L1 lift (2026-05-09). The hook is produced by `createReactBindings`
 * alongside `useSkill` + `useAgentEvent`.
 */
export { useSkillResult } from './useSkill.js';

/**
 * Re-export of the lifted skill-parts helpers.
 *
 * The pure helpers moved to `@canopy/sync-engine-rn/react` 2026-05-09
 * (Phase 41.0 L1). This file stays as a re-export so existing imports
 * (`import { toParts } from '../lib/skillParts.js'`) keep working.
 */
export { toParts, unwrapParts } from '@canopy/sync-engine-rn/react';

.tg-token
.tasks-data/

# Local overrides of tracked tmp/ circle/list configs (real bot tokens etc.)
# To use: copy tmp/<foo>.circle.json -> tmp/<foo>.circle.local.json, fill in secrets.
# Code paths should prefer the .local.json variant when present.
tmp/*.local.json

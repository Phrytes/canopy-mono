{
  "name": "@onderling-app/agents",
  "version": "0.1.0",
  "private": true,
  "description": "agents — 'your agents' management surface over @onderling/agent-registry.  Read slice: list + view (skills / grants / status / lastSeen); P2 control: revoke / grant / revokeGrant / purge (token-first, registry mirrors).",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/cores.js",
    "./manifest": "./manifest.js",
    "./cores": "./src/cores.js",
    "./wireSkills": "./src/wireSkills.js",
    "./defaultCatalog": "./src/defaultCatalog.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@onderling/agent-registry": "file:../../packages/agent-registry",
    "@onderling/app-manifest": "file:../../packages/app-manifest",
    "@onderling/pseudo-pod": "file:../../packages/pseudo-pod",
    "@onderling/sdk": "file:../../packages/sdk"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}

{
  "name": "@canopy-app/import-bridge-v0",
  "version": "0.1.0",
  "private": true,
  "description": "H6 import-bridge V0 — fetches documents from external services (Google Docs / Notion / etc.) and writes them to a Solid pod.  Phase C V0; thin composition of L1a + L1g + L1h substrates.",
  "type": "module",
  "main": "src/index.js",
  "exports": { ".": "./src/index.js" },
  "scripts": { "test": "vitest run", "test:watch": "vitest" },
  "dependencies": {
    "@canopy/core":             "file:../../packages/core",
    "@canopy/vault": "file:../../packages/vault",
    "@canopy/sync-engine":      "file:../../packages/sync-engine",
    "@canopy/identity-resolver":"file:../../packages/identity-resolver"
  },
  "devDependencies": { "vitest": "^2.1.0" }
}

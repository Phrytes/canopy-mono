// Stub for react-native-qrcode-svg. Vite's import-graph analysis
// hits the package's TS sources via the substrate's QrCodeView.jsx
// shim; the runtime test path never renders it.
export default 'QRCode';
